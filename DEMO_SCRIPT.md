# 演示与录屏脚本

目标时长 4–5 分钟。演示重点是“普通用户一句话触发智能体调用视觉工具并获得可追溯结论”，同时如实呈现模型边界。

## 演示前检查

- 使用允许公开展示的演示图片；不要在录屏中暴露测试集目录、文件列表或密钥。
- 关闭通知弹窗，清理终端历史，确认 `.env` 和密钥文件不在画面中。
- 先运行 `validate-submission` 和 32+ 自动测试，保存通过画面。
- 启动 FastAPI 与 Gradio，确认 `/health` 显示四折集成、Qwen和审计状态。

## 分镜与讲解

### 0:00–0:35 场景与痛点

画面：标题页和系统首页。

讲解：水域巡查图片需要专业人员逐张整理、启动模型并解释结果。作品把 YOLO 图像分类封装为 Agent 工具，普通用户上传图片并提出自然语言问题即可完成识别、解释和复核提示。

### 0:35–1:05 架构

画面：方案文档中的架构说明。

讲解：Gradio/FastAPI 接收请求，`WaterAnalysisAgent`受控调用四折 YOLO-CLS 工具，再把结构化结果交给国产开源 Qwen3.6-27B解释。Qwen不参与分类决策，避免生成随机性改变打榜标签；批量链直接复用同一视觉模型。

### 1:05–2:15 正常分析链

画面：上传一张公开演示图片，输入“请分析这张图片中存在什么水域异常，并说明是否需要人工复核”。

操作：点击“开始分析”。依次展示回答、分类与置信度、复核原因、Top-3和工具轨迹。

讲解：工具轨迹明确区分视觉推理、Qwen解释和审计写入；模型版本记录四个折权重。若预测为“乱建”或“正常”，系统根据 OOF 风险证据强制提示人工复核。

### 2:15–2:55 故障降级与安全

画面：临时关闭 Qwen 或使用隔离的故障演示配置，再分析同一张图片。

讲解：远程大模型故障不会丢失 YOLO 结果，系统返回确定性解释并标记降级。上传格式、可解码性、文件大小和像素数均有校验；SQLite 不保存图片、问题或密钥。

### 2:55–3:35 模型证据

画面：模型卡中的 OOF 指标和混淆矩阵摘要。

讲解：四折 OOF Accuracy 97.73%、Macro-F1 73.43%、Balanced Accuracy 71.11%。强调“正常”0/7 是当前短板，以上不是测试集或榜单成绩，项目没有目标框标注，因此准确表述为整图分类。

### 3:35–4:15 提交与复现

画面：终端执行结果校验和交付目录校验；只展示命令结果，不展示测试图片。

讲解：参赛者在本机显式传入测试目录生成 `result.json`。构建器采用白名单打包，只有 `code/`、`design/`、`result/` 三个顶层目录；没有真实结果确认时拒绝生成 `.tar.gz`。

### 4:15–4:40 收束

画面：系统首页与后续路线。

讲解：首版解决单图问答和批量提交；后续在公开合规数据基础上增强少样本类别，并可沿相同工具接口扩展视频抽帧、监控接入和相似案例检索。

## 必拍截图/素材清单

1. `/health`三项状态：四折视觉、Qwen、审计。
2. Gradio完整首页和一次成功分析结果。
3. Top-3、复核原因和三步工具轨迹特写。
4. Qwen故障降级结果。
5. OOF四项核心指标与逐类 Recall。
6. 自动测试、Ruff和依赖检查通过画面。
7. `validate-submission`与`validate-delivery`通过画面。
8. 归档内部只有 `code/ design/ result/` 的列表。

## 禁止表述

- 不说“目标检测到污染物位置”，应说“整图分类识别水域异常”。
- 不把 OOF 指标说成初赛榜单成绩。
- 不说“完全替代人工”；应说“辅助初筛、降低操作门槛”。
- 不展示或朗读 API Key、测试图片清单、测试目录或未公开数据。
# 智能体化升级演示补充（2026-08-21）

演示前使用下列命令启动，确保页面顶部显示“Qwen：已启用”：

```powershell
.\scripts\start_ui.ps1 -EnableQwen -QwenApiKeyFile '.\api key.txt'
```

建议连续展示三种任务，而不是只展示一次分类：

1. 上传图片，输入“请分析这张图片中有什么水域异常。”说明计划为`image_assessment`，仅调用YOLO和简洁分析工具。
2. 保持同一图片，输入“请生成水域巡查报告，并说明是否需要人工复核。”说明标签保持不变，但计划升级为“YOLO → 类别知识 → 巡查报告”。
3. 清除图片，输入“乱堆是什么意思？”说明智能体识别到这是知识咨询，不调用YOLO。

讲解重点：计划、工具轨迹和知识依据是可审计的外显过程；不展示或声称模型内部思维。YOLO负责视觉事实，Qwen负责受控计划与报告，低置信度或“乱建/正常”结果提示人工复核。
