# 水域综合异常识别智能体 Phase 4 验收报告

## 1. 验收结论

Phase 4 工程验收通过，项目具备进入交付与参赛材料阶段的条件。

本结论只覆盖代码、接口、智能体、展示、性能、降级、审计和提交结构校验能力；不包含初赛测试集成绩，也不代表最终榜单分数。验收过程完全未访问、枚举或推理“初赛测试集”。

## 2. 验收环境

- 操作系统：Windows
- GPU：NVIDIA GeForce RTX 4070 Laptop GPU，8GB
- Python：项目独立虚拟环境，兼容3.11/3.12
- PyTorch：2.11.0+cu128，CUDA可用
- FastAPI：0.141.1
- Gradio：5.50.0
- 大模型：赛事官方 `qwen3.6-27b` OpenAI兼容接口
- 视觉模型：四折 `yolo26n-cls` 加权训练模型等权概率集成

## 3. 自动化质量门

| 检查 | 结果 |
|---|---|
| Ruff静态检查 | 通过 |
| Pytest | 32项通过 |
| `pip check` | 无依赖冲突 |
| 源码/测试/README密钥模式扫描 | 未发现硬编码密钥 |
| Qwen故障降级API测试 | 通过，HTTP仍返回200和视觉结果 |
| 提交目录校验器 | 已实现 `validate-delivery` |

## 4. FastAPI现场验收

| 场景 | 结果 |
|---|---|
| `GET /health` | 200；正确报告ensemble、Qwen、audit实际状态 |
| `POST /api/v1/classify` | 200；返回六类之一、Top-3、模型版本与耗时 |
| `POST /api/v1/analyze` | 200；返回回答、结构化结果和工具轨迹 |
| 错误扩展名 | 415 |
| 伪造图片内容 | 415 |
| 像素总数超限 | 413；临时文件已清理 |
| Qwen不可用 | 自动降级，不丢失视觉结果 |

## 5. 现场性能

同一训练来源验证副本用于工程测量，不作为泛化成绩。

| 指标 | 结果 |
|---|---:|
| 首次分类HTTP耗时 | 547.97ms |
| 热分类HTTP中位数（5次） | 90.39ms |
| 热视觉模型中位数（5次） | 82.77ms |
| Agent端到端HTTP中位数（3次） | 1491.46ms |
| Qwen解释范围 | 1178–1702ms |
| SQLite审计范围 | 7.7–9.8ms |
| 3并发分类 | 全部200；约395–530ms/请求 |

现场进程工作集约1.32GiB、私有内存约3.53GiB。系统GPU总显存占用约1377MiB/8188MiB，但驱动未提供单进程拆分，不能把该数值全部归因于本服务。

## 6. Gradio演示验收

- 浏览器真实页面包含图片上传、自然语言问题、回答、分类/置信度、人工复核、Top-3和工具轨迹。
- Gradio官方Client确认端点为2输入、5输出。
- 上传验证副本后约611.52ms返回完整Fake链结果；Fake结果只验证页面协议。
- 页面控制台无error/warning。
- 原Dataframe后处理会触发本机Polars CPU特征错误，已改为Markdown表格并复验通过。
- 内置浏览器的Gradio文件选择器事件未能自动化触发；上传回调已通过Gradio官方Client完成等价功能验收。

## 7. 审计与安全

- SQLite仅记录请求ID、时间、标签、置信度、复核信息、模型版本、总耗时和工具轨迹。
- 不记录图片、图片路径、问题正文、回答正文、API Key或Authorization。
- 审计路径不可写时，核心识别服务继续启动，`/health`报告实际审计状态。
- “正常”和“乱建”因OOF证据不足被列为当前模型的强制人工复核类别。

## 8. 赛事规则映射

| 官方/内部要求 | 状态 |
|---|---|
| 六类标签原样输出 | 通过 |
| `result.json`字段与训练标签同构 | 通过 |
| 国产开源大模型 | 使用赛事 `qwen3.6-27b` |
| 自研深度学习模型作为Agent工具 | 通过 |
| `.tar.gz`内含`code/ design/ result/` | 校验器已完成；最终包在Phase 5生成 |
| `result/result.json` | 生成与校验能力已完成；真实测试结果由参赛者运行 |
| 不泄露密钥 | 通过 |
| 演示展示智能体工具调用 | 通过 |

## 9. 已知限制

- 官方只有整图单标签，没有框或掩码；当前是图像分类，不输出目标位置。
- “正常”仅7张且OOF Recall为0，“乱建”仅4张且Recall为50%，必须人工复核。
- 四模型集成适合低并发演示；3并发时存在GPU资源争用。
- 最终榜单效果仍需参赛者在不向Agent开放测试集内容的前提下执行批量推理并提交。
- 最终 `.tar.gz`、设计书和真实 `result.json` 属于Phase 5交付任务。
