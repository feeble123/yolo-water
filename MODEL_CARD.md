# 水域综合异常识别 YOLO-CLS 四折集成模型卡

## 模型概览

- 任务：整图单标签六分类，不输出目标框或分割掩码。
- 类别：乱采、乱建、乱堆、乱占、有漂浮物、正常。
- 架构：四个 Ultralytics YOLO26n 分类模型，等权平均六类概率。
- 输入：JPG/PNG/WebP 单张图片；批量链默认 320 像素推理。
- 输出：Top-1 标签、置信度、Top-K、模型版本和耗时；交互链附加人工复核策略。
- 用途：水域治理线索初筛与人工巡查辅助，不替代执法认定或现场核验。

## 数据与训练

官方 `train.json` 提供 1,144 条整图标签，无边界框。类别分布为：有漂浮物 1,026、乱占 60、乱采 24、乱堆 23、正常 7、乱建 4。训练采用近重复组隔离的四折交叉验证，避免高度相似图片跨训练/验证折造成泄漏；每折使用类别加权损失（power=0.5，cap=8）训练 15 个 epoch。

没有使用初赛测试集训练、调参或生成模型证据。当前版本未引入外部数据。

## 验证结果

四折 OOF 完整覆盖 1,144 张训练图片，每张只由未见过该图片的折模型产生验证预测：

| 指标 | 结果 |
|---|---:|
| Accuracy | 97.73% |
| Balanced Accuracy | 71.11% |
| Macro-F1 | 73.43% |
| Top-2 Accuracy | 98.95% |

逐类 Recall：乱采 24/24（100%）、乱建 2/4（50%）、乱堆 19/23（82.61%）、乱占 57/60（95%）、有漂浮物 1016/1026（99.03%）、正常 0/7（0%）。这些是训练集分组交叉验证结果，不是初赛榜单成绩。

温度缩放拟合得到 T=1.1603，但 ECE 和 Brier 均变差，首版保持 T=1.0。

## 运行特性

Phase 4 在 RTX 4070 Laptop 8GB 上现场验收：热态单图视觉模型中位耗时约 82.77ms，FastAPI HTTP 中位耗时约 90.39ms；真实 YOLO+Qwen 端到端中位耗时约 1.49s。3 个并发视觉请求均成功，但单请求耗时上升到约 395–530ms，因此当前版本定位为低并发辅助分析，不宣称高吞吐。

## 风险与限制

- “正常”只有 7 张且 OOF Recall 为 0；“乱建”只有 4 张。交互端对这两类无条件提示人工复核。
- 类别极不均衡，Accuracy 会被“有漂浮物”主导，必须同时看 Macro-F1、Balanced Accuracy 和逐类 Recall。
- 单标签监督不能表达同图多异常；预测标签只表示模型的主导类别判断。
- 无框标注意味着不能宣称定位污染物位置、计数、面积或严重度。
- 航拍尺度、季节、天气、相机角度和新水域类型可能造成分布漂移。
- 结果用于线索初筛；涉及处罚、执法或重大告警时必须由有资质人员复核。

## 智能体中的安全使用

视觉模型是唯一分类决策源。Qwen3.6-27B只接收结构化分类结果并生成自然语言解释，不得改写标签；Qwen 超时或故障时，系统保留视觉结论并返回确定性降级说明。审计库不保存图片、路径、问题正文或 API Key，只保存模型版本、结构化结果、复核原因与耗时。

## 版本与可追溯性

四个权重分别来自 `ablation_fold0_weighted_p05_c8_e15`、`fold1_weighted_p05_c8_e15`、`fold2_weighted_p05_c8_e15`、`fold3_weighted_p05_c8_e15`。最终提交包中的 `code/MANIFEST.sha256`记录每个文件哈希。依赖与模型许可证须在提交前按实际锁定版本复核并随开源要求履行相应义务。
# 智能体编排边界（2026-08-21）

本模型卡所述YOLO四折集成仍是六分类标签的唯一视觉事实来源。升级后的`WaterAnalysisAgent`允许官方Qwen提出经过Schema校验的任务计划，并根据只读的YOLO结果、类别知识和复核依据生成分析或巡查报告；Qwen不得修改标签、伪造目标框、数量、位置或违法事实。

不同用户任务可选择不同工具链：图片异常识别、巡查报告、结果可靠性说明和无图片类别知识咨询。Qwen未启用、超时或返回非法计划时，执行器回退为确定性计划，YOLO批量推理和`result.json`生成不调用Qwen，保持赛事结果可复现。
