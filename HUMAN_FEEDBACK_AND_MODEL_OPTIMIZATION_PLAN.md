# 人工纠错闭环与模型准确率优化计划（待确认）

## 0. 目标与边界

目标是把当前“单图识别”升级为可持续改进的行业智能体：用户可以纠正一次判别；系统把这次纠正沉淀为经审核的数据资产；只有在离线评估证明更好时，才发布新的YOLO模型。

当前模型优化阶段的硬约束：**不引入、不下载、不训练任何外部数据集；训练和验证只使用赛事方已提供的官方训练集。** 现阶段的重点转为充分利用既有标注、预训练权重、数据增强、长尾损失、交叉验证和推理集成。未来的人工反馈数据闭环作为独立的后续能力，只有用户日后明确授权保存已纠正图片时才会产生新数据。

本计划**不**让模型或大模型在线自我改权重，不用LLM猜测用户想改成的类别，不访问初赛测试集，也不在未经允许时重新训练或生成新的提交结果。反馈数据只来自用户授权上传或项目依法可用的公开数据，并与比赛测试集物理隔离。

## 1. 产品闭环

```text
YOLO四折集成预测
  → 展示类别、Top-3、置信度、复核原因
  → 用户选择“正确 / 判错，正确类别是…”
  → 本地候选反馈库（待审核）
  → 标签审核、去重、来源与授权核验
  → 版本化训练集 + 固定分组验证
  → 候选模型离线评估
  → 达标才发布；否则保留旧模型
```

## 2. 第一阶段：界面与反馈记录（不训练）

1. 在网页结果下方增加“结果正确”“判别有误”按钮；判错时由人从官方六类中明确选择正确类别，可选填简短原因。
2. 页面保留原始预测、Top-3、置信度和模型版本，明确显示“已记录，待人工审核；本次不会立即改变模型”。
3. 每条反馈生成不可变记录：反馈ID、时间、原始预测概率、人工选定类别、操作人可选标识、原因、模型版本、图片SHA-256、数据来源、授权状态、审核状态。
4. 图片只在用户明确同意“可用于后续训练”时复制到项目内隔离反馈库；否则只保存不含图片的错误统计。绝不保存API密钥、提示词或测试集路径。

## 3. 第二阶段：审核与数据治理

1. 状态流转固定为`pending → approved / rejected / needs_second_review`；稀有类、低清图、多类别图优先二次审核。
2. 对同一图片的重复反馈按SHA-256合并；近重复画面应以“组”为单位进入训练或验证，防止同一场景泄漏到两边。
3. 每条可训练样本必须具备：原图可追溯来源、公开或用户授权证明、单一官方类别、审核人、审核时间、数据集版本。
4. 建立“冻结验证组”：一旦某批数据进入验证，之后的训练不能再使用它；评估继续采用现有近重复组隔离的四折OOF口径。

## 4. 第三阶段：主动学习队列

不是让人随机挑图，而是优先收集最能改变模型的图：

1. **低置信度**：Top-1概率低于先通过OOF校准的阈值（初始可用0.65作为运营筛选值，而非自动结论阈值）。
2. **接近决策边界**：Top-1与Top-2概率差小，例如低于0.15。
3. **四折分歧**：四个模型对类别意见不一致、概率方差大。
4. **弱类定向**：乱建、正常、乱堆、乱采、乱占的疑似图优先；同时保留一部分有漂浮物和正常图，避免新数据只包含“错误”。
5. **多样性去重**：在官方向量嵌入接口完成契约验证后，对候选图检索相似图，优先挑选与已有数据不同的场景、角度、天气和岸线环境。

## 5. 第四阶段：可控再训练

触发条件不是“收到了几条反馈就训练”，而是弱类新增样本达到足以形成多个不同场景组、标签审核完成，并且固定验证集没有被污染。工程上可将每个弱类**先积累至少约30个已审核且相互不近重复的新增样本**作为首个试验门槛；这不是保证效果的魔法数字，样本多样性比数量更重要，乱建/正常最好继续积累更多。

每次试验均固定数据版本、随机种子、四折分组与评估脚本，只变动一个主变量。候选模型只有同时满足以下条件才可替换当前模型：

- 四折OOF的Macro-F1、Balanced Accuracy不低于现冠军；
- 乱建、正常、乱堆、乱采、乱占至少一项有可复现改善，且有漂浮物不出现不可接受退化；
- 逐类混淆矩阵说明改进不是靠把所有图改判为少数类换来的；
- 在独立、冻结的反馈验证组上同样成立；
- 训练可复现，模型、数据、参数、代码版本均可追溯。

## 6. 第五阶段：版本发布与回滚

采用“冠军—挑战者”方式。线上默认仍使用已验证的冠军模型；新模型先离线对照、可选灰度展示，再由人工批准切换。每个版本有模型卡、数据版本、OOF报告和可一键回滚的旧权重。赛事提交与网页模型版本分开编号，避免一次体验优化意外改变已提交结果。

## 7. LLM与向量模型在闭环中的真实职责

- Qwen3.6-27B：解释“为什么需要复核”、把人工原因整理为巡查记录、提醒标签定义；**不拥有改标签或训练模型的权限**。
- Qwen3-VL-Embedding：为已授权图片生成相似度向量，用于去重、主动学习多样性抽样和“相似已纠正案例”检索。
- Qwen3-VL-Reranker：在候选相似案例中重排，帮助审核人员查看更相近的历史纠错。
- YOLO：始终是六分类视觉决策模型；再训练只发生在离线、可验证流程中。

## 8. 准确率实验路线（与反馈闭环并行，先做低风险项）

| 优先级 | 实验 | 解决的主要问题 | 成功判据 |
|---|---|---|---|
| P0 | 官方训练数据/标签/重复组审计 | 稀有类标签错误、近重复泄漏、类别定义不一致 | 冻结数据版本与每类混淆样本清单 |
| P1 | 320→512/640分辨率、保留完整画幅/多裁剪 | 岸线、小目标和角落信息被压缩或裁掉 | 四折OOF弱类召回和Macro-F1提高 |
| P1 | 有节制的分类增强、采样和现有加权损失复核 | 长尾与拍摄变化 | 单变量A/B优于当前冠军 |
| P2 | Class-Balanced、Balanced Softmax、LDAM-DRW、分类头再均衡 | 长尾决策边界 | 不牺牲多数类的情况下提升弱类 |
| P2 | 不同预训练骨干/小型模型集成 | 降低同质模型错误 | OOF概率集成有稳定增益 |
| P3 | 乱采/乱堆/乱建/乱占的框标注与检测分支 | 小而局部的实体或岸线线索 | 有框标注的独立评估明确优于整图分类 |
| P3 | 视觉语言模型作复核候选/相似案例检索 | 补充语义与人工经验 | 仅在冻结验证上验证融合效果，不直接篡改YOLO标签 |

## 9. 各类的具体数据策略

- **乱采**：补齐采砂船、挖掘设备、裸露采坑、作业水域等不同距离和天气的已授权样本；未来可补框标注“设备/采挖区域”。
- **乱堆**：重点收集岸边堆料、建筑垃圾、土石料等小尺度目标；优先验证高分辨率、完整画幅和局部裁剪。
- **乱建**：4张训练图不足以学习稳定视觉定义；先扩展不同建筑/构筑物、岸线位置与背景的已审核样本，再考虑目标框。
- **正常**：不是“没有漂浮物”的单一画面，而是覆盖清水、反光、浪花、船只、岸边设施等硬负例；必须补齐会被误判为漂浮物/乱堆的正常画面。
- **乱占**：补齐围栏、码头、临时设施、岸线占用等边界场景；它依赖环境上下文，优先测试完整画幅和更高分辨率。

## 10. 执行前需要确认

1. 是否先实现第2–4节的“人工纠错与审核数据闭环”，不接入再训练？
2. 反馈图片是否默认不保存，必须由操作者勾选授权后才保存？
3. 谁具备审核/二次审核权限；是否先以单管理员模式实现？
4. 是否授权在只读训练集上执行P0/P1的离线实验？该实验不访问测试集、不生成或提交新结果。
