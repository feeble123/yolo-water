# Progress Log

## Session: 2026-08-21

### Phase 10：仅官方训练集的准确率优化

- 用户已授权进入模型优化实施，并明确禁止引入外部数据集；训练、验证仅使用赛事方提供的官方训练集，初赛测试集继续完全隔离。
- 已在GitHub备份基线`e8229dc`基础上创建实验分支`experiment/fullframe-highres`，后续所有训练产物使用独立实验名称，不覆盖当前四折线上模型或提交结果。
- 本轮实验顺序固定为：P0变换/资源复核 → P1高分辨率单变量 → P2全图letterbox → P3长尾损失 → P4四折OOF与集成。任何候选必须先经分组OOF证明改善。
- P0已完成：记录冠军OOF指标、fold 0可复现超参数、实际Ultralytics分类变换和GPU余量。发现安装版本中`ClassificationDataset`位于`ultralytics.data.dataset`，旧模块路径导入失败，已切换为源码定位方式，不影响现有模型。
- P1第一次训练启动因历史`artifacts/training`目录ACL拒绝创建新子目录而停止，尚未开始epoch、未产生半成品模型。已新建`experiments/training`、`experiments/metrics`和专用运行缓存目录；将以只读原fold数据重新启动。
- 重新启动的前台P1训练被运行环境约30秒时限切断，仅写入epoch 1（Top-1 0.9021）的临时结果，权重未完成保存，不能用于任何结论。后续采用隐藏后台进程和独立日志完成同一固定参数实验。
- 为保持官方训练衍生折只读，已将fold 0的858张训练图与286张验证图复制到`experiments/data/fold_0`；之后的Ultralytics JPEG修复、缓存和训练仅作用于此实验副本。`experiments/`已加入Git忽略规则，不会进入云端源码备份。
- 后台训练保存epoch checkpoint时被Polars CPU特征误判（`unknown feature flag: sse3`）阻断。已在隔离环境验证`POLARS_SKIP_CPU_CHECK=1`可正常导入同一Polars版本；训练入口将在Windows上设置该进程级兼容变量，不改变模型计算或数据。
- P2代码已完成：新增`SquareLetterbox`与保守水平翻转变换，训练CLI新增`--image-transform letterbox`。该实现仅作用于未来候选实验，不影响正在运行的P1默认裁剪训练；3项新变换单元测试和既有长尾测试共6项通过。
- P1 v2已成功越过此前的checkpoint保存故障并持续训练，当前已写入epoch 1–3和`best.pt`/`last.pt`；暂不按中间Top-1选择模型，须等待15 epoch结束并用项目评估器得到逐类指标。
- P1完成并淘汰：512默认裁剪候选的fold 0 Accuracy 0.98951、Balanced Accuracy 0.83268、Macro-F1 0.74935，低于320冠军折基线的0.99301、0.83333、0.83268。虽然乱建单验证图被判对，但正常仍为0，且增加了有漂浮物→乱建误报；不进行四折复训。
- 已扩展评估CLI支持`--image-transform letterbox`，确保P2权重以与训练一致的完整画幅变换评估；评估/变换相关单元测试6项通过、Ruff通过。P2（320 Letterbox、其余条件固定）已在隔离副本后台启动，尚未产生结论。
- P2已完成并淘汰：全图Letterbox候选的fold 0 Accuracy 0.98951、Balanced Accuracy 0.82222、Macro-F1 0.81412，低于320冠军折基线的0.99301、0.83333、0.83268。逐类Recall为乱采100%、乱建100%、乱堆100%、乱占93.33%、有漂浮物100%、正常0%；2张正常仍被判成有漂浮物、1张乱占被判成乱堆。因此不进入四折复训、不替换线上四折模型或现有提交包。
- P3第一项已实现：新增训练期Logit Adjustment损失，按官方训练折的类别先验为logit加入可配置的`tau * log(prior)`；推理仍使用未调整logit，避免在线人为偏置输出。新增2项数值一致性单元测试；长尾与全图变换定向测试8项通过，Ruff通过。下一步在默认320裁剪、其余参数完全固定的fold 0运行`tau=0.25`单变量对照。
- P3a已完成并淘汰：Logit Adjustment（`tau=0.25`）的fold 0 Accuracy 0.98951、Balanced Accuracy 0.83268、Macro-F1 0.82698，低于冠军折基线0.99301、0.83333、0.83268；仅平衡准确率近似持平，正常Recall仍为0。未替换线上四折模型、未生成新提交结果；下一项为LDAM-DRW损失对照。
- P3b已实现：新增LDAM-DRW损失与参数校验。LDAM按训练折类别样本数为稀缺类设置较大的目标类别间隔；DRW在第10个epoch后才启用既有的上限逆频率权重。新增2项边距/延迟重加权数值测试，长尾与全图变换定向测试10项通过，Ruff通过。下一步在默认320裁剪、其余参数固定的fold 0运行15 epoch单变量对照。
- P3b已完成并淘汰：LDAM-DRW的fold 0 Accuracy 0.97902、Balanced Accuracy 0.79935、Macro-F1 0.71736，低于冠军折基线0.99301、0.83333、0.83268；乱占Recall降至80%，正常仍为0。按止损规则，停止继续尝试损失函数，转入训练集错例/标签可辨性和四折OOF概率决策分析。
- P3.5已完成并拒绝上线：新增嵌套OOF的保守类别偏置评估，禁止正常/乱建（样本不足）参与偏置拟合，且每一holdout折均不参与偏置选择。四折均选择全零偏置，整体指标无任何提升。训练图片抽样显示正常类与有漂浮物类含有相似的清漂作业船/船载垃圾场景，不能擅自改写官方标签。全量62项pytest通过，Ruff通过；当前线上四折模型和提交结果保持不变。
- P3.6已完成：新增`build-label-review` CLI，从官方训练集的四折OOF错例生成只读人工复核清单。已生成`experiments/review/official_training_label_review.json`，含20张候选：全部7张正常错例、2张乱建、4张乱堆、3张乱占和4张有漂浮物。清单记录本地路径、官方标签、预测、概率和复核状态字段；不复制、不修改任何官方图片或标签。

## Session: 2026-08-20

### Phase 5：真实智能体启动修复与提交包重建

- 用户发现7862界面对训练集前13张样本连续给出错误结果。只读诊断确认项目根目录没有`.env`，而旧`start_ui.ps1`会触发`Settings.use_fake_model=true`默认值；该Fake模型按图片哈希生成伪随机结果，不能用于真实识别。
- 直接以四折真实权重和与打榜一致的320尺寸复测官方训练记录`00000.jpg`、`00002.jpg`至`00013.jpg`，13/13与`train.json`一致；四个权重的六类名称映射完全一致，排除权重、类别索引和集成标签映射错误。
- 修复真实启动链：`start_ui.ps1`默认查找开发/发布两套四折权重，固定320尺寸、设备0、项目内Ultralytics/Matplotlib缓存和Windows字体变量；仅显式`-UseFakeModel`才启用测试替身，Qwen改为`-EnableQwen -QwenApiKeyFile`显式开关。
- 增加视觉尺寸/设备配置，单图Agent与批量链统一推理参数；Gradio顶部显示真实四折YOLO或测试替身模式及Qwen状态。更新README和部署手册，明确Gradio可独立运行，FastAPI仅用于REST对接。
- 使用更新后的真实启动脚本在7867端口验收：HTTP 200、页面显示“真实四折YOLO集成”和“Qwen未启用”；真实单图Agent返回`00000.jpg`为“有漂浮物”98.5%。用户实际在7865的真实Qwen页面复测也确认结果正确。
- 回归通过：Ruff全绿、41项pytest通过、`pip check`无冲突、Windows PowerShell 5.1脚本解析通过；7867临时验收服务已停止，用户7865真实页面仍保留。
- 已重建并独立审计最终归档`X-M_水域综合异常识别_初赛正式提交.tar.gz`，大小11,746,943字节，SHA-256为`2e82af60ee5f95342c2b80dacdc03c332f737dbc085590980ca812a4ede3931b`。旧归档已被修复版替换，可通过构建脚本再次生成。

### Phase 5：初赛正式提交包生成

- 用户确认以现有六分类成果完成初赛提交，并授权严格按赛事方结构打包；继续不访问初赛测试集、不读取API Key。
- 用户本机生成的`result/result.json`通过独立校验：695条记录、`valid=true`、0错误。
- 首次白名单构建发现敏感赋值扫描把空白`.env.example`跨行误判；已将空白限定为水平空格并增加空值允许测试。
- 第二次构建发现Python运行时密钥读取被误判为硬编码；现区分配置文件非空赋值和Python字符串字面量硬编码，动态Secret读取允许、真实字面量仍拒绝。
- 交付状态文档曾保留“结果/归档未完成”的历史描述；已在正式归档前更新README、交付报告和最终检查清单，避免评委看到过期状态。
- 最终回归：Ruff通过、40项pytest通过、`pip check`无冲突、结果Schema有效。
- 已生成`X-M_水域综合异常识别_初赛正式提交.tar.gz`，大小11,745,422字节，SHA-256为`eb56be37418e7cf5fcdc951ba81d772eff0928693118b0ac3bdafa5d3f6b5b39`。
- 独立只读归档审计通过：顶层仅`code/ design/ result/`，73个代码文件、2个方案文件、4个模型、695条结果；72条代码清单哈希全部匹配，敏感文件0、链接0、过期状态0、队伍名`X-M`。
- 已删除先前包含过期状态文档的候选归档，只保留名称含“正式提交”的最终文件。提交平台前仍需用户在Word中逐页视觉检查DOCX，并核对官方群最新通知。

### Phase 5：队伍身份定稿与本地批量推理交接

- 用户确认报名队伍名为`X-M`，并要求提供详细的本地运行与批量推理方法。
- 本轮将只更新生成版方案DOCX，不改动官方原模板；随后执行结构、表格几何、可访问性和占位符审计。
- 本地推理说明将以现有`generate_submission.ps1`和`predict-batch`真实参数为准；继续不访问、枚举或读取初赛测试集。
- 方案DOCX已写入`参赛队伍：X-M`，当前SHA-256为`5adeb5adfa39637ab726f393ab048185d2694684e4a043e21df094ca2cdcb6e1`；结构审计确认队伍字段精确匹配1处、身份占位符0处。
- 文档回归仍为82段、4表、1节；表格精确几何全部通过，可访问性审计高/中/低问题均为0，官方原模板哈希保持不变。
- 新增`LOCAL_BATCH_INFERENCE_GUIDE.md`，覆盖环境/GPU/权重检查、测试目录由用户输入、图片计数与同名检查、旧结果备份、四折推理、Schema/数量/标签/文件名核对、审计文件边界和最终打包。
- 本轮最终质量门通过：Ruff全绿、37项pytest通过、`pip check`无冲突、全部PowerShell脚本语法通过；方案身份占位符门通过且当前最终归档数仍为0。
- 用户在Windows PowerShell 5.1运行批量脚本时，UTF-8无BOM中文提示被错误解码并破坏引号，触发ParserError。已将5个运行脚本改为纯ASCII提示，避免依赖BOM或终端代码页；全部`.ps1`已通过Windows PowerShell 5.1解析，37项测试、Ruff与`pip check`均通过。


### Phase 5：交付与参赛材料启动

- 用户授权开始下一阶段开发；Phase 5 已进入进行中。
- 本阶段交付范围为：可复现部署脚本、官方模板方案文档、模型卡、演示/录屏材料、严格白名单提交包构建器与最终合规检查。
- 保持数据边界：完全不访问、枚举或推理“初赛测试集”；`api key.txt`仅由运行时按路径读取，任何文档、日志、测试和压缩包均不得包含密钥。
- 最终`.tar.gz`只能使用用户在测试集上生成并通过Schema校验的真实`result.json`；缺失真实结果时构建器必须明确拒绝，不创建伪提交物。
- 已按`documents`技能进入官方模板跟随流程：保留原始`AI智能体设计方案.docx`不变，先蒸馏模板结构，再基于副本填写并执行DOCX渲染与逐页视觉核验。
- 首次把模板QA中间文件放入遗留`artifacts/`时遇到ACL拒绝；只读结构审计仍成功确认A4纵向、单节、左右1.25英寸/上下1英寸，且无图片、域或内容控件。后续QA改用当前会话专用可写目录，不再重复该失败路径。
- 当前依赖环境确认缺少LibreOffice/`soffice`，标准模板渲染失败。按文档流程切换到结构QA回退；另将失败的内联Python检查改为版本化只读检查脚本，避免PowerShell多行转义问题。
- 首次方案DOCX构建在官方模板缺少列表样式/编号部件时停止，尚未写出目标文件。构建器已改为模板兼容的分组正文，不使用伪造Unicode项目符号或手工编号；文档操作开始标记已执行且不会重复。
- 第二次DOCX构建在Markdown表格分隔行正则编译时停止，仍未写出目标文件；已改为逐单元格确定性判断，未重复失败表达式。
- 第三次DOCX构建发现官方模板同样缺少`Table Grid`样式，仍未生成目标文件；现由构建器直接写入表格边框与全部几何参数，不依赖不存在的模板表格样式。
- 第四次构建在设置单元格宽度为`None`时被`python-docx`拒绝，仍未生成目标；已删除该对象层操作，仅保留OOXML精确宽度配置。
- 官方模板方案DOCX现已成功生成，SHA-256为`03f773cb51f7fa17cc629b25c70749dfe9b300471b50d5125bfdcb0a4376a69d`。结构审计确认82段、4表、单节A4、14包部件；原模板哈希未变化，可访问性审计0项问题。
- 当前方案保留1处“提交前填写报名队伍名”；这是未获用户信息时的显式槽位，不是虚构身份。逐页PNG视觉核验仍因环境缺少`soffice`未完成。
- Phase 5首轮回归：35项pytest全部通过、PowerShell脚本语法全部通过、`pip check`无冲突；Ruff发现2项文档构建脚本规则问题已修复。
- 本地元数据确认当前Ultralytics 8.4.123声明AGPL-3.0；新增第三方组件合规说明。`pip show`因终端GBK无法输出包摘要中的emoji产生日志编码错误，后续改用`importlib.metadata`。
- 沙箱内同步editable依赖因网络受限失败一次；经授权联网后成功安装`python-docx 1.2.0`与`lxml 6.1.2`并刷新项目安装。
- 交付构建器进一步增加身份占位符阻断、文本密钥赋值扫描和友好JSON错误输出；方案构建脚本支持`--team-name ... --force`显式更新生成版，且永不允许覆盖官方原模板。
- 最终回归当前为Ruff全绿、37项pytest通过、`pip check`无冲突、5个PowerShell脚本语法通过；CLI拒绝门现场通过且无归档副作用。
- Phase 5工程交付已收口为`PHASE5_DELIVERY_REPORT.md`。部署脚本与演示材料完成；最终文档身份、外部视觉复核、真实结果和`.tar.gz`仍等待用户侧信息/操作，阶段保持进行中。
- 文档技能的表格几何审计发现4张表均只有一项一致性问题：表格缩进120 DXA与单元格起始边距100 DXA不等。已修正构建器并准备重生成方案。
- 方案已重生成，当前SHA-256为`043ecc5a25e4cc5d8d860cad2db29bd1565e996109e0e9068d71fbe08bd1b5d1`；4张表全部通过`tblW/tblInd/tblGrid/tcW`精确几何审计。
- Phase 5本轮最终门再次通过：Ruff全绿、37项pytest通过、`pip check`无冲突、5个PowerShell脚本语法通过；方案可访问性审计仍为0项问题，结构为82段/4表/1节，确认仅保留1个队伍名槽位且当前最终归档数为0。


### Phase 4：系统测试与赛事验收启动

- 用户授权开始下一阶段；已按 `planning-with-files` 恢复计划，并选用本地浏览器验证真实 Gradio 页面。
- 本阶段验收顺序：FastAPI接口与错误路径 → Gradio真实页面与交互 → 性能/稳定性/降级/审计 → 赛事交付结构与可复现报告。
- 验收样本只使用 `artifacts/data/grouped_folds` 中的训练来源验证副本；继续完全排除“初赛测试集”，API Key仅由运行进程按文件路径加载。
- Phase 4基线：Ruff全绿、26项pytest通过、`pip check`无冲突；PyTorch 2.11.0+cu128、CUDA可用，设备为RTX 4070 Laptop，FastAPI 0.141.1、Gradio 5.50.0。
- Fake FastAPI首次启动在初始化审计库时因项目遗留ACL无法创建`artifacts/acceptance`目录而停止，尚未绑定端口。接口契约测试将关闭审计以隔离职责；SQLite写入另做受控验收，不重复失败命令。
- 关闭审计后Fake FastAPI在127.0.0.1:8001启动成功；health、classify、analyze均200，错误扩展名和伪造图片均按契约返回415。接口使用的图片来自fold 0验证副本，未触碰初赛测试集。
- Uvicorn PTY会话未响应两次Ctrl+C；已仅终止启动日志确认的PID 37108并验证进程退出，未影响其他Python进程。
- 将现场接口检查固化为FastAPI集成测试，并修复像素超限分支未删除临时文件的问题。首次测试收集暴露默认审计库不可写会阻断模块导入，现改为审计失败安全降级，并在health返回实际`audit_enabled`。
- 修复后Ruff全绿、29项测试通过。真实FastAPI在8002端口启动，四折/Qwen/审计状态全部正常；热分类HTTP中位数90.39ms、模型82.77ms，真实Agent端到端中位数1491.46ms，3次调用全部成功。
- SQLite脱敏字段检查通过，3条真实分析事件均可追溯且无敏感列；3并发分类全部成功，但耗时升至约395–530ms，记录为当前单GPU服务的容量边界。
- Gradio Fake演示服务已在127.0.0.1:7861启动；浏览器本地测试环境已连接，并按文件选择器流程准备使用训练来源验证副本进行UI交互验收。
- 浏览器首次等待`networkidle`被当前运行时拒绝；页面标签已创建，不重开页面，改用`domcontentloaded`和关键控件可见性判定。
- Gradio页面标题及全部结果控件可见性通过。首次按隐藏`input[type=file]`触发文件选择器超时并重置了浏览器控制会话；不重复该方式，下一次使用页面可见上传按钮。
- 浏览器控制已恢复，原Gradio页面仍在会话tab 4中；文件上传测试将改用可见的`Upload file`按钮并对事件超时做显式捕获。
- 可见`Upload file`按钮仍未触发当前内置浏览器的文件选择器事件，并再次重置会话。按不重复失败原则，浏览器验收结论限定为真实页面结构/控件可见；上传与动态回调改用Gradio Client API验证。
- Gradio官方Client成功发现`/lambda`端点，但首次调用在Dataframe后处理阶段触发本机Polars CPU特征错误。已将Top-3和轨迹组件改为Markdown表格，避免可选Dataframe依赖。
- 修改后的UI尝试重启到7861时报告端口仍占用（尽管IPv4监听查询为空）；停止在同一端口重试，改用7862完成验收，阶段结束时按端口/PID核验清理。
- Gradio 7862回调验收通过：2个输入、5个输出，上传验证副本后约611.52ms返回完整回答、分类、复核、Top-3和轨迹；Polars问题修复有效。
- 浏览器复验7862页面通过，修复后全部关键控件和两个Markdown结果区均正常渲染。
- 页面控制台0条error/warning；真实API进程工作集约1.32GiB、私有内存约3.53GiB。GPU总显存占用1377/8188MiB，但驱动不提供单进程拆分，报告中将保持该限制说明。
- 真实FastAPI正常完成应用关闭；Gradio会话停止后，8002/7861/7862三个验收端口均无残留监听进程。
- 新增`validate-delivery`校验器及2项测试，覆盖官方code/design/result结构、result.json格式和敏感文件禁入；新增API级Qwen降级测试。当前32项pytest通过、pip依赖无冲突；Ruff仅剩2处导入顺序，已修正待复验。
- 文档一致性审计确认早期架构仍写Qwen-Agent/yolo26s，与实际实现不符。首次跨文件报告补丁因findings历史行上下文不匹配而整体未应用，现改为小补丁逐项更新。
- 已统一架构为实际的WaterAnalysisAgent受控编排与四折yolo26n-cls集成，并生成`acceptance_report.md`。
- Phase 4最终质量门：Ruff全绿、32项pytest通过、pip依赖无冲突、全部CLI命令注册成功。Phase 4完成，下一阶段为Phase 5交付与参赛材料。

### Phase 3B：指标证据复核与真实智能体链路

- 已按 `planning-with-files` 工作流恢复 `task_plan.md`、`progress.md`、`findings.md` 与会话上下文。
- 当前工作拆为两条同步证据链：先核实官方图像级标签、OOF Recall 的计算来源和术语边界；再安全接入官方 Qwen API，完成真实“指令→视觉分类工具→解释”端到端验证。
- 数据边界保持不变：只使用训练资料和既有衍生产物，完全不访问、枚举或推理“初赛测试集”；API Key 文件内容不读取、不输出、不写入日志。
- 已直接核验 `train.json`：1,144 条、六类整图标签、无框/掩码字段；确认现有模型是 YOLO 分类模型，不是目标检测模型。
- 首次读取 OOF 指标时误假设 `per_class` 位于 JSON 顶层导致 `KeyError`；随后仅检查结构，确认指标实际位于 `metrics.per_class`，未修改任何产物。
- 已从 OOF 混淆矩阵复算六类 Recall：24/24、2/4、19/23、57/60、1016/1026、0/7，与已报告百分比逐项一致。
- 已检查官方接口案例（未读取密钥文件）：接口为 OpenAI 兼容 `/v1`，模型名 `qwen3.6-27b`，支持 LangChain 非流式/流式对话；现有适配器基本协议一致，下一步补强运行可靠性并做真实调用。
- 已为 Qwen 适配层增加 30 秒默认超时、1 次有限重试、问题长度上限、空响应检查、脱敏异常、无内容健康检查；Agent 在 Qwen 故障时保留 YOLO 结构化结果并返回确定性解释。
- 新增 `verify-qwen` 与 `analyze-image` CLI，分别用于官方接口连通性验证和真实单图 Agent 链路；密钥仍仅由环境变量或文件路径在运行时加载。
- 新增 Qwen 载荷边界、错误脱敏、健康检查和 Agent 降级测试；当前 21 项测试全部通过，Ruff 使用 `--no-cache` 检查全绿。
- 默认 Ruff/compileall 写缓存被当前目录遗留权限拒绝；Ruff 已以 `--no-cache` 成功验证。compileall 写 `__pycache__` 失败属于缓存目录权限问题，源代码已由 pytest 实际导入执行验证，后续避免把该缓存写入作为验收条件。
- 沙箱内首次官方 Qwen 健康检查因网络限制返回脱敏“服务暂不可用”；按规则获得联网权限后重试成功，模型 `qwen3.6-27b`，延迟约 3701.46ms。未读取或输出密钥内容，也未输出模型回复正文。
- 为避免 CLI 导入 FastAPI 模块时触发全局 `app` 构建、重复加载四个 YOLO 模型，将 `build_agent` 抽离到无副作用的 `runtime.py`；回归结果仍为 21 项测试通过、Ruff 全绿。
- 首次真实 Agent 命令在推理前因指定的 `artifacts/matplotlib` 缓存目录不存在而停止，未访问图片模型或远程接口；已改为复用现有 `artifacts/ultralytics_config` 后重试，待核验 E2E 结果产物。
- 已生成 `artifacts/e2e/real_agent_validation_sample.json`：四折 YOLO 分类成功、Qwen 超时后安全降级成功，证明视觉结果不会因远程服务故障丢失。
- 将 Qwen 设置为90秒且关闭重试的第二次命令约31秒后结束，但未生成目标产物且终端无错误输出；暂不将其视为成功，下一步先核对配置解析和进程退出码，再决定是否继续调用，避免盲目消耗接口额度。
- 已纠正上述长进程判断：命令在30秒后进入后台会话，项目 Python 进程继续运行；未重复调用。最终产物 `artifacts/e2e/real_agent_validation_sample_qwen_retry.json` 已生成，Qwen约79.98秒后仍降级，配置解析确认是90秒/0重试。
- 新增脱敏错误类别和 `verify-qwen --explanation` 诊断模式；短生成诊断返回 `EmptyResponse` 而非超时/鉴权错误。现已显式关闭Qwen thinking模式，待做协议兼容验证。
- 关闭 thinking 模式后诊断成功（约842ms、53字符）；完整真实 Agent 链路也已成功，产物为 `artifacts/e2e/real_agent_validation_sample_success.json`，四折视觉约613ms、Qwen约1046ms。
- E2E结果暴露一项安全策略缺口：模型对“正常”给出70.51%并未触发复核，但该类OOF Recall为0。下一步将复核规则从单一置信度升级为“置信度 + 类别验证风险”。
- 已将“正常/乱建”设为当前模型版本的强制复核类，新增2项策略测试；总计23项测试通过、Ruff全绿。
- 最终真实 Agent 证据为 `artifacts/e2e/real_agent_validation_sample_review_policy.json`：视觉约610.64ms、Qwen约1240.37ms、两步均success，Qwen按结构化复核原因生成安全解释。
- 完成Gradio展示界面、SQLite脱敏审计、`serve-ui`命令及README运行说明；界面 Blocks 构建冒烟通过。
- 新增审计隐私、UI格式和模型版本追溯测试；当前26项测试通过。首次Ruff仅发现测试导入顺序，已手工修正，待最终复验。
- Phase 3实现阶段完成并转入Phase 4。最终复验：Ruff全绿、26项pytest全部通过、CLI命令注册完整；下一步是API/UI现场启动、性能与赛事交付结构验收。

### Phase 3：模型评估与长尾优化启动

- 用户授权开始下一阶段。
- 阶段顺序冻结为：可复现评估器 → 多数类基线 → 单折长尾策略对照 → 四折训练与集成 → 批量预测CLI。
- 当前首要退出条件：冒烟权重产出Macro-F1、Balanced Accuracy、每类precision/recall/F1、混淆矩阵和逐图概率记录。

### 项目目录统一与训练集恢复

- 用户指定所有作品产出统一放入`E:\GPT-Codex\Yolo-water\水域综合异常识别`；已将源码、虚拟环境、配置、文档、模型权重、artifacts、result及官方ZIP全部移入，父目录只保留该项目目录。
- 迁移后已修复`.venv`可编辑安装路径；包从新目录导入，CUDA可用，6项测试、Ruff和`pip check`全部通过。
- 用户重新解压训练集后，JPEG原始特征已恢复；发现旧四折目录仍共享硬链接，已删除4,579个仅由项目生成的危险衍生条目，训练集保持存在。
- 已用独立文件副本重新生成四折目录，并新增`verify-folds`命令，强制检查缺失、文件大小和共享文件实体。
- 全量隔离验证通过：4,576个衍生条目，缺失0、共享文件实体0、大小不一致0。
- 已完成恢复后的YOLO26n分类GPU冒烟训练：fold 0、320像素、batch 16、1 epoch约20秒，验证Top-1 95.1%、Top-5 99.7%，最佳权重已生成。该数字仅证明链路可用，不作为最终模型成绩。
- 冒烟训练后再次核验官方训练集：仍为1,144张，缺少标准JPEG尾标记的数量保持239，证明第三方自动修复仅发生在独立副本。

### Phase 1：需求与赛事调研
- **Status:** complete
- Actions taken:
  - 明确当前只做需求调研和方案可行性评估。
  - 记录不得访问“训练集”和“初赛测试集”的硬性边界。
  - 建立项目计划、调研记录和进度日志。
  - 只列出赛事资料根目录顶层条目，未进入两个排除目录。
  - 读取赛事官网首页，记录赛道定位、赛程和初赛/决赛基本机制。
  - 尝试读取参赛 Q&A；微信文档被浏览器站点安全策略拦截，已停止该访问路径。
  - 尝试渲染本地 Word 方案；因环境缺少 LibreOffice/soffice 未生成页面图，转为结构化提取。
  - 读取 `readme.txt`，确认 6 类标签与最终压缩包目录结构。
  - 创建只读 DOCX 结构提取辅助脚本 `.tmp/inspect_docx.py`。
  - 修复辅助脚本 UTF-8 输出并读取 Word 模板结构，确认五个方案章节及效果评估项。
  - 尝试打开水域赛道独立详情链接；页面自动回到首页，未获得更多详情。
  - 读取 `train.json` 结构，确认共有 1,144 条整图单标签记录且无目标框/掩码标注。
  - 统计标签、字段、尺寸与顺序；确认数据极端长尾，最大类占 89.69%，最小类仅 4 张。
  - 定向搜索赛事官方域名，并核验榜单/报名路由；未找到公开评分公式或进一步技术细则。
  - 形成产品目标、角色、MVP 功能、扩展范围、非目标、数据/模型、非功能、内部验收和赛事待确认项的需求基线草案。
  - 完成 DOCX 读取后删除临时提取脚本；保留的项目文件只有需求计划与调研记录。
  - 用户提供参赛 Q&A 正文，补齐模型、数据、平台、评审和提交次数规则。
  - 确认官方认可“自行开发深度学习模型并包装为 Agent 工具”，与用户总体架构一致。
  - 核验 Qwen3.6-27B 官方开放权重仓库，更新国产开源模型、平台迁移、数据合规、演示和刷榜需求。
  - 用户授权在约10天周期内采用压缩式设计并立即转入实现，Phase 1需求基线冻结。

### Phase 2：系统与技术方案设计
- **Status:** in_progress
- Actions taken:
  - 核验Qwen-Agent、Ultralytics分类、FastAPI和Gradio官方能力。
  - 检测本机GPU：RTX 4070 Laptop，8GB显存；CPU/内存查询被权限拒绝。
  - 确定Agent、视觉模型、后端、演示界面和训练/推理进程的技术栈方向。
  - 创建 `architecture.md`，冻结总体架构、技术栈、工具契约、API、项目结构、模型策略、测试策略和10天计划。
  - 用户授权只读访问训练集，并明确初赛测试集由用户独占查看与运行。
  - 安全核对官方API材料：密钥文件仅验证非空；提取OpenAI兼容基地址、模型名与调用参数，未泄露密钥。
  - 只读核验训练集：1,144张JPEG、约657MB，与标签清单完全一一对应。
  - 查看“有漂浮物”与“正常”各一张代表样例，确认任务具有明显的全局场景/事件语义；分类预处理应保留全图上下文，并防范设备、桥梁和拍摄地点等背景捷径。
  - 查看“乱采”与“乱堆”代表样例；确认异常常位于岸线而非水面，且工程设施/砂石语义相近，需以少数类召回率、混淆矩阵与热力图作为重点验证手段。
  - 查看“乱占”与“乱建”代表样例；确认类别取决于水域与耕作/建筑的空间关系。将Top-2候选、置信度校准与低置信度人工复核列为首版协议。
- Files created/modified:
  - `task_plan.md`（updated）
  - `findings.md`（updated）
  - `progress.md`（updated）
  - `architecture.md`（created）
- Files created/modified:
  - `task_plan.md`（created）
  - `findings.md`（created）
  - `progress.md`（created）

## Test Results

- `python -m compileall -q src tests`：通过，首版新增Python文件无语法错误。
- 新工程密钥模式扫描：未发现硬编码密钥；唯一命中是运行时从`SecretStr`向API客户端传值的预期代码。
- 依赖导入测试：失败，当前捆绑Python尚未安装FastAPI等项目依赖；下一步创建项目独立`.venv`并安装锁定范围内依赖。
- 已在工作区`.venv`成功安装项目及开发依赖；首次完整测试为`4 passed`。
- 首次Ruff检查发现7项问题（字符串拼接、临时文件上下文、FastAPI默认参数、类型注解和导入顺序），正在逐项修复。
- PyTorch探测结果为`2.13.0+cpu`且`cuda_available=False`；必须切换官方CUDA构建后才能开始GPU训练。
- 已切换并实测`torch 2.11.0+cu128`：CUDA 12.8可用，设备为RTX 4070 Laptop GPU，显存8GB；GPU矩阵运算通过。
- Pillow已固定回11.3.0，`pip check`报告无依赖冲突；完整测试4项通过，Ruff全绿。
- 训练集重复审计发现5组完全重复、81组近重复，共覆盖478张；冻结“近重复组不得跨训练/验证”的切分约束。
- 发现赛事JSON尺寸字段必须为字符串；已修正提交Schema和测试。4张乱建标签尺寸与实际图像不符，模型输入采用实际图像，提交尺寸由用户运行时实际解码读取。
- 已按81个近重复组生成4折YOLO分类目录：747个独立组，每折286张验证图，六类均覆盖且近重复组无跨折泄漏。首次实现使用硬链接，后发现第三方库会修复JPEG，已永久改为独立复制并增加同文件拒绝保护。
- 新增可复现模型评估器与`evaluate` CLI，输出完整概率、逐类指标、混淆矩阵和多数类基线；单元测试增至9项，Ruff全绿。
- 完成fold 0一轮冒烟权重正式评估：Accuracy 0.9510、Balanced Accuracy 0.4654、Macro-F1 0.5048；乱建与正常Recall均为0，确认下一步必须进行长尾对照。
- 实现有上限的逆频率幂次类别加权损失及训练CLI参数，新增3项单元测试，总计12项通过，Ruff全绿。
- 完成同条件1 epoch类别加权初筛：Balanced Accuracy提升至0.5778，但Macro-F1降至0.4919；进入15 epoch标准/加权收敛对照，暂不定版。
- 完成fold 0的15 epoch严格A/B：加权模型Accuracy 0.9930、Balanced Accuracy 0.8333、Macro-F1 0.8327，三项均小幅优于标准损失；选加权策略进入其余三折训练。
- 完成4个分组折的15 epoch加权训练和逐折正式评估；所有最佳权重均已保存。
- 完成1,144张无重复OOF汇总：Accuracy 0.9773、Balanced Accuracy 0.7111、Macro-F1 0.7343；正常类Recall仍为0。
- 实现温度拟合及NLL/Brier/ECE评估；因T=1.1603仅改善NLL却恶化Brier和ECE，首版决定不启用校准。
- 实现四模型等权概率集成，已同时接入Agent单图分类器和赛事`predict-batch` CLI；默认温度固定为1.0。
- 使用fold 0验证副本完成四模型批量工程冒烟：286条提交JSON通过格式校验，0错误；该结果仅验证工程，不作为有训练泄漏的性能结论。
- 更新README与`.env.example`，提供四权重Agent配置及由参赛者自行填写输入目录的批量预测/校验命令。
- 阶段收尾验证：17项测试全部通过，Ruff全绿；下一步转入官方Qwen API安全实测、真实Agent端到端和展示界面。
| Test | Input | Expected | Actual | Status |
|------|-------|----------|--------|--------|

## Error Log

- 2026-08-20：捆绑Python执行核心依赖导入时报告`ModuleNotFoundError: fastapi`。原因是项目刚创建、尚未安装依赖；不修改系统环境，改在工作区创建独立虚拟环境。
- 2026-08-20：沙箱内首次pip安装因网络权限失败；按环境规则经批准后联网安装到工作区`.venv`成功。
- 2026-08-20：默认PyPI安装得到CPU版PyTorch；停止训练动作，改为核验并安装PyTorch官方CUDA构建。
- 2026-08-20：首次训练命令在Ultralytics尝试写用户Roaming配置目录时被拒绝，训练并未启动；改为在训练入口将`YOLO_CONFIG_DIR`固定到项目`artifacts/ultralytics_config`。
- 2026-08-20：第二次训练命令在Matplotlib字体初始化时因沙箱缺失`WINDIR`失败，仍未启动训练；在Windows变量缺失时恢复真实系统目录语义，并将`MPLCONFIGDIR`固定到项目artifacts。
- 2026-08-20：首次真正进入Ultralytics数据扫描时发现其会“修复并保存”缺少标准JPEG尾标记的图片；由于衍生折目录使用硬链接，导致234张原训练图在12:40:14–12:40:25被改写。已立即中止训练，未访问测试集；代码改为独立复制并拒绝硬链接目标。原图恢复需用户明确授权后从官方ZIP定向恢复训练条目。
- 2026-08-20：项目迁移后可编辑安装路径失效，且Hatchling/editables未安装到持久虚拟环境；补齐构建组件并将`.pth`精确更新至新`src`路径后恢复。
- 2026-08-20：迁移后的旧`artifacts`在沙箱内继承不可写权限；经明确限定目标并提升权限后，仅删除危险硬链接目录并重建独立副本。
- 2026-08-20：首次执行真实模型评估时，沙箱拒绝创建`artifacts/metrics/smoke_fold0_recovered`；评估逻辑和参数已通过前置检查，改为对同一条项目内评估命令申请受控提权重跑。
| Timestamp | Error | Attempt | Resolution |
|-----------|-------|---------|------------|
| 2026-08-20 | 微信文档 Q&A 被站点安全策略阻止 | 1 | 不绕过；请求用户后续提供导出文件或正文 |
| 2026-08-20 | DOCX 渲染器找不到 LibreOffice/soffice | 1 | 改用结构化提取；不声称完成视觉页码核验 |
| 2026-08-20 | DOCX 结构化提取中文乱码 | 1 | 让辅助脚本显式使用 UTF-8 标准输出后重读 |
| 2026-08-20 | `git status`：当前目录不是 Git 仓库 | 1 | 记录环境事实；需求阶段不再调用 Git |
| 2026-08-20 | WMI/CIM查询CPU与内存被拒绝 | 1 | 不提升权限；GPU信息足够首轮选型 |
| 2026-08-20 | 未发现系统Python命令 | 1 | 实现时创建项目级Python 3.11环境 |
| 2026-08-20 | 代表样本路径抽取PowerShell空管道语法错误 | 1 | 改为数组收集后单独输出 |
| 2026-08-20 | 代表样本查询的数组计数表达式解析失败 | 2 | 简化为每类首个样本查询 |
| 2026-08-20 | 全量衍生副本检查的一行式Python引号转义失败 | 1 | 不重复临时命令；实现正式`verify-folds` CLI并全量验证通过 |

## 5-Question Reboot Check
| Question | Answer |
|----------|--------|
| Where am I? | Phase 2：系统与技术方案设计 |
| Where am I going? | 形成可直接实现的架构、技术栈、接口与10天交付计划 |
| What's the goal? | 在8月底前交付可打榜、可演示、可复现的水域异常智能体 |
| What have I learned? | 见 `findings.md` |
| What have I done? | 已建立需求调研的文件化基线 |
# 下一轮榜单反馈（待授权优化时执行）

- 用户已提交初赛作品，官方反馈综合得分为 **81.59**；逐类成功率：有漂浮物 94%、乱占 73%、乱采 40%、乱堆 7%、乱建 0%、正常 0%。
- 该反馈来自官方隐藏测试集，项目侧不读取或查看初赛测试集；当前无法从逐图预测直接定位错误样本。
- 后续优化优先级：先补强“正常、乱建、乱堆、乱采”四个弱类，同时保持“有漂浮物、乱占”的能力；重点验证类别极不均衡、稀缺类别场景覆盖不足、图像分辨率与隐藏集域偏移，而非仅继续堆叠模型。
- 候选路线（尚未执行）：合规公开数据扩充与标注说明、困难样本/类别均衡采样及损失函数、较高分辨率/多尺度与裁剪、差异化模型集成、基于训练集的错误分析与消融对比；每次打榜保留可复现记录，避免凭榜单反馈盲调。

### 智能体化升级计划启动（2026-08-21）

- 用户反馈：当前网页启用/未启用Qwen差异不明显，要求将轻量封装升级为用户可感知的行业智能体，并要求先给出详细计划再执行。
- 已核验真实基线：当前Agent固定先调用YOLO；Qwen只基于结果JSON生成短解释，既不参与路由也不改变视觉结果。
- 已在`task_plan.md`增加Phase 6–10：先冻结受控多工具智能体设计，再实现Qwen结构化编排、工具层、对话与接口、验证演示；榜单精度优化独立为后续阶段。
- 当前阶段：Phase 6进行中。下一步仅做需求/契约细化与现有代码影响面盘点，尚未改变产品功能。
- 已完成Phase 6并新增`AGENT_UPGRADE_DESIGN.md`：冻结四类用户任务、Qwen结构化计划、本地白名单执行器、YOLO视觉事实独占、知识库边界、UI/API展示内容、回退策略和验收用例。
- 当前阶段已转入Phase 7：开始实现计划器、工具契约与受控执行器；赛事批量推理链保持隔离且暂不修改。
- 已完成Phase 7影响面盘点：现有API和测试依赖`AnalysisResponse.answer/result/trace`，因此升级将采用向后兼容的Schema扩展；先为计划与白名单执行新增纯单元测试，真实YOLO/Qwen仅用于后续端到端验收。
- Phase 7首次定向测试：4项失败，均为接口演进兼容问题——旧断言假定分类在轨迹首位，旧模拟Qwen只接受`explain(prompt, result)`。未重试同一版本；已保留旧解释器两参数调用路径，并将断言升级为验证`plan_task → classify_water_image`新流程。
- 已实现Phase 7第一段：新增`TaskIntent`、受控`TaskPlan`、`LabelGuidance`和兼容扩展响应Schema；新增确定性计划器、Qwen JSON计划器、计划校验/回退逻辑与版本化类别知识工具。
- `WaterAnalysisAgent`现执行“计划 → YOLO分类 →（按计划）知识查询/复核 → Qwen报告或本地回退”；YOLO的不可变分类结果仍为唯一标签来源。
- 新增计划器和Qwen计划解析回归测试；定向执行15项测试通过，Ruff全绿。下一步进入Phase 8，升级UI/API以显式展示计划与工具证据。
- 已完成Phase 8实现：网页新增任务计划、知识与核查依据面板；同一图片任务可选择不同工具链。无图片的类别咨询会调用Agent的`consult`链，不加载YOLO。API新增`/api/v1/guidance/{label}`和`/api/v1/consult`，既有`/api/v1/analyze`保持兼容。
- 新增UI/API无图片咨询测试并完成20项定向测试与Ruff检查。Phase 7与Phase 8均完成，进入Phase 9全量回归与真实Qwen验证。
- 计划文件更新首次因跨文件补丁中的历史行不匹配而整体拒绝；未重复同一跨文件补丁，改为逐文件定位后分别更新，已成功。
- Phase 9真实Qwen无图片咨询：沙箱内首次因网络限制回退为`APIConnectionError`；按受控联网授权重试后，官方Qwen成功生成`label_guidance`计划，轨迹为`plan_task:success → lookup_label_guidance:success`，未读取或输出密钥内容。
- 真实“巡查报告”首次端到端尝试在模型加载前发现开发目录没有`models/fold*.pt`打包权重；不重复该路径，改用已知存在的`artifacts/training`四折训练权重。此次失败未加载图片、未访问初赛测试集、未调用Qwen。
- 使用训练图片进行官方Qwen端到端验收的受控联网请求被安全策略拒绝，理由是该行为可能向外部服务发送训练数据相关信息；立即停止且不采用任何规避手段。后续改为人工构造的非数据集结构化分类结果，仅验证Qwen报告生成协议；真实YOLO本地链路继续由不联网回归测试覆盖。
- 真实官方Qwen报告生成已用人工构造的“有漂浮物”结构化结果与本地类别知识完成：返回非空巡查报告（61字），未发送任何训练/测试图片或密钥。结合此前真实Qwen计划成功，完成安全分段验收。
- 完成全量质量门：51项pytest通过、Ruff全绿、`pip check`无冲突。已现场启动真实四折YOLO网页并通过浏览器验证新增任务计划、知识依据与工具轨迹面板；无图片“乱堆是什么意思？”咨询显示`label_guidance`确定性计划且未调用YOLO。临时7868服务和测试页已关闭。
- 已更新README、architecture、DEMO_SCRIPT和设计方案Markdown，并据此重建`design/水域智巡智能体设计方案.docx`。DOCX内容结构核验通过（85段、4表、升级关键字段均存在、身份占位符0）；渲染器因环境缺少LibreOffice/soffice失败，未完成视觉PNG验收，仍需用户在本机Word逐页目视复核。
- 已补充DEPLOYMENT与MODEL_CARD的智能体升级边界和启动说明。智能体化升级Phase 6–9完成；Phase 10准确率优化保持独立，避免把模型打榜实验与Agent功能升级混为一轮变更。
- 修复类别知识咨询的自然语言别名：原实现只匹配官方完整标签“有漂浮物”，导致“漂浮物是什么意思？”无法命中。现已支持“漂浮物、水面垃圾、采砂、违建、堆放、占用、无异常”等常用表达，并新增“漂浮物→有漂浮物”回归测试。首次检查仅发现升级后遗留的未使用导入，已删除后待复验。
