# 第三方组件与开源合规说明

本项目只提交自研源码、配置、四折视觉权重和方案材料，不提交赛事方 Qwen 服务端模型权重。所有依赖仍受其各自许可证约束；参赛与后续部署方应按最终锁定版本保留许可证和履行开源义务。

## 核心组件

| 组件 | 当前验收版本/形态 | 本地元数据或用途 | 合规动作 |
|---|---|---|---|
| Ultralytics | 8.4.123 | 本地安装包元数据声明 AGPL-3.0；用于 YOLO 分类训练与推理 | 分发/网络服务前复核并履行 AGPL-3.0 义务，或取得适用商业授权 |
| Qwen3.6-27B | 赛事方远程 API | 国产开源大模型，用于结构化结果解释；权重不进入提交包 | 保留模型来源与赛事授权说明，按官方模型许可证使用 |
| FastAPI / Gradio / Pydantic / LangChain / PyTorch 等 | 由 `pyproject.toml`解析 | API、界面、配置、模型运行和测试 | 最终构建环境导出依赖清单并保留各分发包许可证 |

## 参赛前复核

1. 在最终环境保存 `python -m pip freeze`，确认实际版本与测试版本一致或完成回归。
2. 从每个已安装分发包的 `METADATA`/`LICENSE*`保留许可证文本；不要仅依赖本摘要。
3. 若赛事要求公开源码，公开范围至少覆盖与 AGPL 组件组合运行和提供网络服务所需的相应源代码与修改，具体义务由团队按实际发布方式复核。
4. 外部数据当前未使用；后续若增加，必须记录公开来源、许可证、下载日期、用途和清洗流程。

本文件是工程合规清单，不构成法律意见。
