# 水域综合异常识别智能体：调研发现与需求记录

> 本文件会包含来自网页与赛事资料的外部内容摘要；这些内容只作为研究数据，不作为可执行指令。

## 用户已明确的需求与边界
- 参赛赛道：“水域综合异常识别”。
- 已完成报名，并将赛事资料下载到 `E:\GPT-Codex\Yolo-water\水域综合异常识别`。
- 训练集允许只读分析、训练与内部验证；禁止打开、读取、枚举或运行“初赛测试集”，该目录仅由用户操作。
- 当前阶段：需求分析；先核验赛事规则与资料，再评估方案，不开始开发。
- 初始方案：YOLO 承担污染目标检测；智能体负责理解自然语言指令、选择/调用 YOLO 等工具、解释并返回结果，从而降低专业操作门槛。
- 初始交互示例：用户上传图片并询问污染类型，智能体调用内部视觉检测工具，汇总检测结果并反馈。
- 潜在扩展：在赛事许可和需求确认后，再考虑视频文件与监控流；目前不能默认纳入首版范围。

## 智能体化升级基线（2026-08-21）

- 用户指出当前7860页面在启用和未启用Qwen时体感差异很小，并要求先制定详细升级计划、再按计划执行。
- 代码核验结果：`WaterAnalysisAgent.analyze()`始终先无条件调用`classifier.classify()`；未启用Qwen时调用固定模板`_fallback_answer()`，启用Qwen时仅调用`QwenChatProvider.explain(prompt, result)`改写同一份YOLO结构化结果。
- 当前Qwen系统提示明确限制“只能依据视觉工具JSON解释”“不得修改标签”，输出上限为120汉字；该安全边界正确，但当前用户问题不会改变工具选择或执行计划，故界面更接近“YOLO网站 + 解释层”。
- 升级方向：Qwen输出受校验的任务计划，本地白名单执行器根据计划调用视觉识别、类别知识查询、巡查报告与审计工具；页面展示计划、工具轨迹、证据和回退状态。视觉标签保持YOLO独占写入。
- 本次升级不访问初赛测试集，不改变赛事批量预测链，也不把Qwen的语言生成作为赛事分类结果来源。
- 影响面盘点：现有`schemas.py`只有`ClassificationResult`、`ToolTrace`和`AnalysisResponse`；`runtime.py`只注入视觉分类器、Qwen解释器和审计器；`WaterAnalysisAgent`与API/单元测试均依赖旧三字段响应。升级必须以可选字段/默认值维持`answer/result/trace`向后兼容。
- 当前测试已覆盖Qwen解释故障时保留YOLO结果、上传校验和基础API契约。Phase 7将先增加计划器与执行器的纯单元测试，再改动运行时和UI，避免把真实YOLO或官方API纳入核心逻辑测试。
- Phase 7实现证据：受控`TaskPlan`强制四种意图与精确工具序列；Qwen只可提出计划，非法JSON、非法工具链、未启用或服务失败均回退为本地计划。定向pytest（15项）与Ruff均通过。

## Research Findings
- 官网首页将赛事定位为“AI＋城市治理”，并写明参赛者应“基于官方指定基座大模型开展行业智能体研发”。
- TRACK_03“水域综合异常识别”的官网描述：融合高点视频监控与多源遥感数据，实现水域漂浮物、河道“四乱”、非法采砂等异常事件的自动识别、分类与实时告警，支撑河湖监管智慧化决策。
- 官网公布每队 1–5 人，可跨专业、院校、企业和行业组队。
- 官网赛事流程（页面当前显示）：报名 6 月 27 日–8 月 24 日；初赛打榜 8 月 1 日–8 月 31 日；线上决赛 9 月 1 日–9 月 30 日；颁奖 10 月 23 日。
- 初赛：本地使用官方指定基座大模型开展智能体离线研发；推理后上传标准 JSON 预测结果并触发自动测评，按总分排名晋级。
- 决赛：在官方云端环境调试、推理和优化；测试集加密封闭，只允许在在线环境内读取，并实时在线推理打分及留痕。
- 对赛事官方域名的定向搜索未发现独立公开的评分公式、基座大模型名称、提交次数限制、晋级队伍数量或智能体技术合规细则；搜索结果仅复现首页信息。

## Q&A Confirmed Rules
- 全部团队成员必须分别完成个人报名；仅队长报名会导致团队审核失败并影响开发与榜单权限。
- 团队人数以报名页面为准；官网当前公示为每队 1–5 人，允许高校、企业和科研机构混合组队。
- 报名审核通常为 1–3 个工作日；材料或赛道错误被驳回后可修改重提。
- 核心评审侧重：行业智能体实用性、场景适配能力、模型迭代优化效果、完整落地解决方案；未提供各项具体权重。
- 初赛开发平台可自由选择，但只能使用国产开源大模型；官方提供远程大模型接口。决赛须在赛事提供的国产平台上开发。
- 官方数据集可合规使用；扩展数据必须公开、标明来源，禁止违规、涉密、私有或商用受限数据。
- 允许使用自行微调的模型，但模型必须为开源模式，禁止闭源模型。
- 初赛采用刷榜赛制，可重复提交，每日最多 5 次。
- 官方提供 Qwen3.6-27B；也允许选择其他国产开源大模型。
- 官方明确认可选手自行开发深度学习模型，并将其包装成 Agent 的一个工具。
- 后续评测阶段需提交完整项目报告和模型运行演示材料。
- Q&A 第14条只说明赛题四的得分点，与本项目的赛题三评分方式无关，不能类推使用。
- Qwen 官方 Hugging Face 组织存在 `Qwen/Qwen3.6-27B` 开放权重仓库，Apache-2.0 许可；它是带视觉编码器的多模态模型。赛事远程接口是否开放其图像输入能力仍需确认。

## Official API Materials
- `api key.txt` 已确认存在且非空（20字节）；未读取或回显其内容。
- 官方接口基地址为 `https://www.zsjsry.top/v1`，示例中使用尾部斜杠，两种形式应由客户端统一规范化。
- 官方示例使用 `langchain_openai.ChatOpenAI`，模型名为 `qwen3.6-27b`。
- 示例演示流式 `llm.stream(...)` 与非流式 `llm.invoke(...)`，参数包含 temperature=1、top_p=0.95、max_tokens=1024、presence_penalty=0、frequency_penalty=0。
- 示例未展示原生Function Calling/Tools字段，也未展示Embedding和Reranker调用格式；相关能力在真实接入前必须通过不含敏感输出的契约测试确认。
- 密钥加载顺序设计为：环境变量优先，其次用户配置的密钥文件；既有 `api key.txt` 不复制、不改写、不提交。

## Local Material Findings
- 资料根目录包含“训练集”和“初赛测试集”；训练集已获只读授权，初赛测试集仍为绝对禁止访问范围。
- 允许读取的顶层文件有：`AI智能体设计方案.docx`、`readme.txt`、`train.json`。
- `readme.txt` 指定 6 类标签：乱采、乱建、乱堆、乱占、有漂浮物、正常。
- 数据分为训练集与测试集；训练标签为 `train.json`，测试集输出格式必须与训练标签格式一致。
- 最终提交为自定义名称的 `.tar.gz`；内部必须包含 `code/`（全部项目代码）、`design/`（按规范编写的智能体方案设计文档）、`result/result.json`（模型预测结果）。
- `AI智能体设计方案.docx` 是一份空白模板，要求填写参赛队伍、作品名称、完成日期，并覆盖五个主体章节：需求与痛点分析、智能体总体架构设计、核心模块详细设计、创新点设计、效果评估；效果评估至少列出“评估结果”和“任务执行耗时”。
- Word 模板未发现表格、页眉或页脚；因缺少 LibreOffice，纯视觉排版与准确页码未核验。
- `train.json` 顶层是 1,144 个样本组成的数组；每个样本当前可见字段为 `filename`、`width`、`height`、`label`。
- 样本标签是整张图片级的单一类别；未发现目标框坐标、分割掩码或多标签列表。
- 六类分布极端不均衡：有漂浮物 1,026（89.69%）、乱占 60（5.24%）、乱采 24（2.10%）、乱堆 23（2.01%）、正常 7（0.61%）、乱建 4（0.35%）。最大类与最小类相差 256.5 倍。
- 所有 1,144 条记录字段集合一致、文件名唯一、宽高均为正；图像尺寸范围为 640–4624 像素宽、500–4080 像素高，最常见尺寸为 1920×1080（473 张）。
- JSON 顺序具有明显类别聚集，不能直接按原顺序切训练/验证集；必须采用分层策略并关注近重复/同源场景泄漏。
- 用户授权后只读核验训练集：目录为单层平铺结构，共1,144张 `.jpg`，总大小688,678,687字节（约657MB）。
- 训练集文件名与 `train.json` 1,144条记录完全一一匹配：无缺失标签图片、无未标注额外图片、无重复文件名。

## Training Sample Visual Findings

- `00000.jpg`（有漂浮物）：画面包含大量漂浮/岸边堆积物、吊装设备、车辆或船体等完整作业场景。异常并非只能靠水面小目标判断，设备、岸线和处置作业也可能成为模型线索。
- `00033.jpg`（正常）：宽阔、干净水面与桥梁、植被岸线、远处建筑共同构成全局场景，类别依据主要是“未见明显异常”。
- 初步结论：任务更接近保留全局上下文的图像级场景分类，而不是纯粹的小目标检测。训练预处理应保持长宽比并尽量保留全图，避免激进中心裁剪；同时要防范模型把吊车、车辆、桥梁、拍摄地点等背景当成类别捷径。
- `02052.jpg`（乱采）：水面大体干净，类别信息主要来自岸边采砂/装卸设施、传送带、货车与船舶组合，属于事件级、关系级语义。
- `02060.jpg`（乱堆）：异常主要是右岸的砂石或建材堆放，所占面积有限，水体本身没有明显污染；与乱采都可能出现砂石、道路和工程设施，存在高混淆风险。
- 工程含义：不能把视觉工具限定为“水面污染检测器”；它应识别涉水场景中的六类综合异常。内部评估必须报告少数类召回率和完整混淆矩阵，并用 Grad-CAM 等热力图检查模型是否关注到岸线异常区域。
- `02043.jpg`（乱占）：大尺度水域/滩涂与耕作地、植被或小型设施并存，异常对象边界不清晰，类别更依赖“占用水域空间”的空间关系，而非单个显著目标。
- `02486.jpg`（乱建）：航拍视角下建筑紧邻狭窄河道，类别语义是建筑与河道的邻接关系；建筑本身并不是一般视觉数据中的异常物。
- 最终视觉结论：六类是“水域综合异常”，不等于六类污染物。YOLO-CLS 可作为快速基线，但类别定义、置信度校准、Top-2候选和低置信度人工复核必须进入产品协议。尤其“乱建”仅4张，4折交叉验证中每折验证仅1张，应同步探索公开数据增广、度量学习或多模态检索，且不能把单次验证结果当作稳定结论。
- 训练集重复审计：5组字节级完全重复；按同标签64位dHash汉明距离≤6聚类得到81个近重复组，共覆盖478张图片。有漂浮物近重复覆盖456张，最大组113张；普通随机分层切分会产生严重同源帧泄漏，必须采用组隔离切分并同时报告随机切分结果仅作对照。
- 官方`train.json`中`filename/width/height/label`四个字段均为字符串。提交Schema必须保持`width/height`为正整数字符串，不能因内部图像库使用整数而改变JSON类型。
- 4张乱建图片的标签声明尺寸与实际文件尺寸均不一致，且不是简单宽高交换。训练应以实际解码图像为准；生成赛事结果时应读取目标图片实际尺寸并输出字符串，不能仿照这4条错误尺寸。
- Ultralytics在扫描时会对缺少标准JPEG结束标记的文件执行自动修复并保存。衍生数据绝不能使用硬链接指向官方原图；必须使用独立副本，并通过`os.path.samefile`保护拒绝共享文件实体。首次冒烟扫描暴露该行为并改写了234张训练原图，训练已立即停止，待用户授权后从官方ZIP定向恢复。

## Feasibility Assessment
- 固定 6 类、1,144 个图像级单标签样本使监督分类可行，但样本量不大，类别分布和域差异会显著影响泛化。
- 当前官方标注不支持直接训练 YOLO 目标检测模型。若坚持 YOLO 家族，可优先考虑 YOLO-CLS；若要输出检测框，必须另行人工标框或引入弱监督定位，而比赛自动评分仍需映射回官方图像级标签。
- “YOLO 目标检测”不应在需求阶段被锁死为唯一模型；应将需求定义为“可替换的视觉识别工具”，再通过基线实验决定 YOLO-CLS、通用分类模型或混合方案。
- 极端类别失衡是当前首要建模风险：只预测“有漂浮物”即可获得约 89.69% 的表面准确率，因此 accuracy 不能单独作为内部验收指标。
- “国产开源大模型 Agent 调用自研视觉模型工具”的总体思路已被 Q&A 明确认可，合规可行性高。
- 仅做一个机械的聊天包装层虽然可能满足最小工具调用形式，但不利于实用性、场景适配和完整方案评分；首版需要体现输入校验、意图理解、工具选择、低置信度复核、批量任务、结果解释、日志追踪和提交物生成。
- Qwen3.6-27B 本身具有多模态能力，可在后续设计阶段评估其作为解释/复核辅助的价值；为保证可评测与可复现，赛事六分类标签仍应由版本化视觉工具产生，除非实验能证明融合方案更优。
- 本机GPU为 NVIDIA GeForce RTX 4070 Laptop、8GB显存，适合训练轻量/小型YOLO分类模型、混合精度推理和多折顺序训练；不适合本地承载27B大模型，因此Agent必须使用赛事远程API。
- 当前没有可直接调用的系统Python命令；实现需要创建项目独立Python 3.11环境并锁定依赖。
- Ultralytics当前分类任务直接输出整图类别与置信度，和赛事JSON标签粒度匹配；官方同时警告默认RandomResizedCrop/CenterCrop可能裁掉极端长宽比图像的重要区域，因此本项目必须采用保持比例的Resize/Letterbox预处理。
- Qwen-Agent官方框架支持自定义工具注册、Agent内部工具调用和OpenAI兼容模型服务配置，适合在短周期内对接赛事Qwen API并封装视觉识别工具。
- FastAPI适合提供上传、推理、健康检查接口；重型训练不应放入Web后台任务，训练和批量比赛推理使用独立CLI进程。
- Gradio Chatbot/Blocks支持在对话中呈现图像等组件，适合快速构建比赛演示界面，不另起React前端。

## Draft Requirements

### 1. 产品目标
- 面向非算法专业用户，提供自然语言驱动的水域异常识别智能体；用户不需要了解模型命令、权重路径或预处理参数。
- 同时覆盖“赛事自动评测”和“现场演示”两条主流程：前者生成严格 JSON，后者展示智能体如何自主调用视觉工具并解释结果。
- 产品术语统一使用“水域异常”而非仅称“污染”；六类中乱采、乱建、乱堆、乱占属于河湖管理异常，并非都能准确归为污染。

### 2. 角色
- 普通用户/河湖监管人员：上传图片并用自然语言询问异常类型。
- 参赛开发者：配置模型、批量推理、查看评估与日志、生成提交包。
- 智能体：解析意图、校验输入、选择视觉工具、调用推理、解释输出、在不确定时给出提示而非臆断。

### 3. 首版功能范围（建议作为 MVP）
- FR-01 单图输入：接受一张常见格式水域图片，并校验文件类型、尺寸和可读性。
- FR-02 自然语言问答：支持“分析这张图片”“有什么异常/污染”等等价表达。
- FR-03 工具编排：智能体自主选择并调用视觉分类工具；用户无需手工执行模型命令。
- FR-04 六类输出：结果必须落入乱采、乱建、乱堆、乱占、有漂浮物、正常之一。
- FR-05 可解释反馈：返回预测类别、置信度、简短依据和风险提示；若启用热力图/弱定位，必须明确其解释性质。
- FR-06 低置信度处理：低于阈值时标记“建议人工复核”，不得强行给出确定性描述。
- FR-07 批量赛事推理：读取指定图片清单，按原顺序输出每图结果；不丢图、不重名、不改文件名。
- FR-08 标准结果生成：生成与官方 `train.json` 结构一致的 `result.json`，字段为 filename、width、height、label。
- FR-09 可审计日志：记录请求标识、时间、所用模型/权重版本、工具调用、耗时和结果；不得记录不必要的原图内容或敏感信息。
- FR-10 错误反馈：针对无效图片、模型未加载、资源不足、推理超时和结果写入失败返回可行动的错误信息。
- FR-11 大模型适配：支持赛事 Qwen3.6-27B 远程接口，并保留替换其他国产开源大模型的能力。
- FR-12 演示模式：能够可视化呈现用户指令、Agent 决策、视觉工具调用、结果与总耗时，供模型运行演示材料使用。
- FR-13 模型迭代对比：至少能展示多数类基线、首个训练模型和优化模型之间的关键指标变化，支撑“模型迭代优化效果”评分。

### 4. 后续扩展范围（不默认承诺首版）
- 批量图片交互式分析与统计报告。
- 视频文件抽帧、时序聚合、重复告警抑制。
- RTSP/监控流接入、实时告警、事件去重和持续跟踪。
- 多源遥感影像、地理位置、时间和气象等上下文融合。
- 精确目标框或分割；需要新增标注或合法外部数据支持。

### 5. 明确非目标
- 首版不承诺仅凭现有图像级标签训练出可信的检测框。
- 首版不把大模型自身的视觉猜测作为最终赛事分类结果；最终标签来自可版本化、可评估的视觉工具。
- 首版不自动执行处置、执法或对外告警，只提供识别与建议人工复核。
- 在评分规则未确认前，不以单一 accuracy 作为模型完成标准。
- Agent不读取、枚举或运行初赛测试集；只提供用户本地执行的批量预测与JSON校验命令。

### 6. 数据与模型要求
- DR-01 官方六类名称必须原样保留，建立稳定的内部类别 ID 映射。
- DR-02 初赛推理输出必须逐图生成与 `train.json` 同构的记录。
- DR-03 训练/验证划分必须分层，并检查相邻帧、近重复图像或同一监控源导致的数据泄漏。
- DR-04 少数类过少时采用交叉验证/留一法，并报告结果不确定性。
- DR-05 训练流程应允许类别权重、重采样、针对性增强或其他长尾学习方法，并记录其影响。
- DR-06 数据、权重、配置和代码版本应可追溯；固定随机种子并保存训练配置。
- DR-07 外部数据必须公开可访问，并记录数据集名称、来源链接、许可证、使用范围和预处理方式；禁止私有、涉密、违规或商用受限数据。
- DR-08 使用人工补标、伪标签或数据生成前，需要确认其产物是否必须公开，以及是否属于允许的外部数据扩展。
- DR-09 Agent 使用的大模型必须为国产开源模型；闭源国内外模型均不得进入参赛运行链路。
- DR-10 自研视觉模型、预训练权重及其依赖必须满足赛事开源要求和对应许可证；具体开源交付范围待确认。

### 7. 非功能要求
- NFR-01 可复现：从干净环境可依说明完成依赖安装、训练/加载权重、推理和结果生成。
- NFR-02 可替换：智能体通过稳定工具接口调用视觉模型，允许在不改对话层的情况下替换 YOLO-CLS 或其他分类模型。
- NFR-03 性能：记录单图端到端耗时、纯模型耗时、显存和内存占用；目标阈值待官方云环境信息确认。
- NFR-04 稳健性：单个坏文件不应导致整批任务失败；失败样本应被记录并明确返回。
- NFR-05 安全与隐私：仅处理用户明确提供的图像；限制文件大小和类型；不得让文件名或图片元数据触发任意命令。
- NFR-06 可观测：工具调用与异常有结构化日志，能够追溯某个结果来自哪个模型版本。
- NFR-07 平台兼容：初赛可在任意平台开发并使用赛事远程模型接口；决赛必须迁移至赛事提供的国产平台，业务逻辑不得与本地环境或单一 API 强耦合。
- NFR-08 运行降级：视觉分类与批量 JSON 生成不能因远程大模型接口临时不可用而完全失效；Agent 交互层应报告服务状态并保留可恢复机制。
- NFR-09 合规清单：交付前自动或人工核对大模型来源、视觉模型许可证、外部数据来源和依赖许可证。
- NFR-10 密钥安全：API密钥不得出现在源码、命令行参数、异常堆栈、日志、SQLite、模型卡或提交压缩包中。
- NFR-11 测试集隔离：批量预测CLI不得输出图片预览或样本内容；项目开发和自动测试不触碰测试集路径。

### 8. 内部验收口径（待官方指标覆盖/修订）
- AC-01 给定任意有效单图请求，智能体在一次交互内完成工具调用并返回六类之一、置信度和复核提示。
- AC-02 给定无效输入，系统不调用模型且返回明确错误。
- AC-03 批量结果条数、顺序、文件名、宽高与输入清单严格一致，JSON 可被标准解析器读取。
- AC-04 内部评估至少输出宏平均 F1、每类 precision/recall/F1、混淆矩阵、accuracy 和推理耗时。
- AC-05 “全部预测有漂浮物”的多数类基线必须被显式记录；候选方案需在宏平均指标和少数类召回上证明改进。
- AC-06 相同模型版本与输入在容许数值误差内给出可复现结果。
- AC-07 最终压缩包包含 `code/`、`design/` 和 `result/result.json`，并可在交付前通过自动结构检查。
- AC-08 参赛运行链路中不存在闭源模型调用；Agent 大模型和视觉模型来源、版本、许可证均可追溯。
- AC-09 每次候选提交先通过本地 JSON、样本数和标签枚举校验；按照每日最多 5 次的额度管理提交记录和榜单结果。
- AC-10 演示材料能完整展示自然语言输入、Agent 工具调用、识别结果、异常处理和任务耗时，而非只播放预先录制的静态结果页。

### 9. 赛事证据缺口/必须确认
- Q-01 Qwen3.6-27B 远程接口的协议、鉴权、限流、上下文、图像输入、费用和可用期是什么？
- Q-02 是否要求提交 Agent 大模型本身的权重，还是仅提交调用代码/配置并在决赛平台使用官方模型？
- Q-03 初赛总分的公式、公开榜/私有榜比例、是否采用 accuracy、宏 F1 或其他指标？
- Q-04 已确认每日最多 5 次；仍需确认初赛总提交上限、结果文件大小、JSON 字段类型（宽高是字符串还是整数）及排序要求。
- Q-05 已确认允许公开外部数据和开源自训/微调模型；仍需确认预训练权重、数据生成、伪标签、人工补标、模型集成及衍生标注是否必须公开。
- Q-06 决赛云环境的 GPU/显存、CPU、内存、磁盘、操作系统、Python/CUDA 版本和单次推理时限？
- Q-07 决赛国产平台是否允许联网、安装依赖、调用赛事模型 API 或下载视觉模型权重？
- Q-08 已确认会关注实用性、场景适配、模型迭代、完整方案，并要求项目报告和运行演示；仍需确认自动分类分数、智能体演示、方案与创新性的具体权重。
- Q-09 “高点视频监控与多源遥感、实时告警”是愿景背景、决赛必做项，还是加分项？
- Q-10 测试集是否可能含多异常共存；若有，单标签真值如何定义优先级？

## Fold 0 冒烟模型正式评估（2026-08-20）

- 权重：`artifacts/training/smoke_fold0_recovered/weights/best.pt`，仅训练1个epoch。
- 数据：近重复组隔离后的`fold_0/val`衍生副本，共286张；未访问初赛测试集。
- Accuracy 0.9510，高于多数类恒预测“有漂浮物”的0.8951，说明模型已获得一定有效区分能力。
- Balanced Accuracy 0.4654、Macro-F1 0.5048，显著低于表面Accuracy，确认长尾问题是下一轮训练的首要矛盾。
- 每类Recall：乱采0.3333、乱建0、乱堆0.6667、乱占0.8、有漂浮物0.9922、正常0。
- “乱建”验证集仅1张、“正常”仅2张，单折指标方差极大；最终判断必须汇总四折OOF预测，不能以单折单样本下结论。
- 当前模型从未预测“乱建”或“正常”；长尾方案必须提升这些类别进入决策边界的机会，同时控制对多数类精度的破坏。
- 本次端到端平均约98.39ms/张，包含模型/数据管线启动成本，不等同于稳定态单张延迟。

## Technical Decisions

## 标签与 OOF 指标证据复核（2026-08-20）

- 官方根目录 `train.json` 共 1,144 条记录，每条仅含 `filename`、`width`、`height`、`label` 四个字符串字段；不存在 bbox、坐标、mask 或 segmentation 字段。因此现有监督任务是整图六分类，不是目标检测或实例分割。
- 官方标签分布为：有漂浮物 1,026、乱占 60、乱采 24、乱堆 23、正常 7、乱建 4。模型训练标签来自该 JSON，数据准备只是把已标注图片复制到类别目录以适配 Ultralytics 分类数据格式，没有从图片内容猜标签。
- `artifacts/metrics/oof_weighted_p05_c8_e15/oof_metrics.json` 明确记录 `fold_count=4`、`unique_sample_count=1144`；逐类 Recall 来自四折组隔离验证的 OOF 预测汇总，不是初赛测试集结果，也不是四模型对训练集直接推理的结果。
- OOF 混淆矩阵逐类证据为：乱采 24/24=100%；乱建 2/4=50%；乱堆 19/23=82.61%；乱占 57/60=95%；有漂浮物 1016/1026=99.03%；正常 0/7=0%。Recall 定义为该类正确识别数除以该类真实样本数（TP/(TP+FN)）。
- 术语应统一为“YOLO 图像分类识别”或“YOLO-CLS”。只有新增框/掩码标注并训练 detection/segmentation 模型后，才能声称输出异常位置。
- 现有 `QwenChatProvider` 已按官方示例使用 LangChain `ChatOpenAI`、官方 `/v1` 基地址和 `qwen3.6-27b`，但尚缺连接性探测、超时/重试边界、可诊断但不泄密的异常封装及真实端到端证据。
- 官方调用案例只证明 OpenAI 兼容的普通对话与流式调用，未展示原生 Function Calling。因此首版采用可审计的本地 Agent 编排：程序确定性调用 YOLO 分类工具，再把结构化工具结果交给 Qwen 解释；不把视觉分类决策交给大模型篡改。
- 真实最小连接测试成功：官方 `qwen3.6-27b` OpenAI 兼容接口返回正常，单次健康检查端到端延迟约 3.70 秒。测试输出不含回复正文、请求载荷或密钥，证明现有适配协议与官方服务兼容。
- 首次四折集成真实 Agent 工程验证完成本地视觉环节：对训练来源的 fold 0 验证副本，四模型输出“正常”70.51%，视觉耗时约746ms。Qwen生成环节在默认30秒、1次重试下累计约60.7秒超时，Agent按设计返回结构化视觉结果并标记 `qwen_explain=fallback`。该样本被其他三折模型训练见过，只能作为工程链路验证，不能作为泛化性能证据。
- 第二次把客户端超时放宽到90秒、关闭重试后，视觉推理约636ms，Qwen仍在约79.98秒后失败并安全降级。结合3.70秒健康检查成功，可排除密钥、域名和基础协议错误；当前主要风险是生成请求耗时/服务端生成限制，需要缩短生成上限或改为流式读取后再验证。
- 将生成上限降为192 tokens后，结构化解释诊断在约7.93秒返回 `EmptyResponse`，确认服务已接收请求但最终 `content` 为空。首版解释任务不需要长链推理，因此按Qwen兼容接口的 chat template 参数显式关闭 thinking 模式，目标是让有限 token 用于最终短回答。
- 显式关闭 thinking 模式后，结构化解释诊断成功，Qwen耗时约842ms并返回53字符；完整四折 YOLO-CLS + Qwen 链路随后成功，视觉工具约613ms、Qwen解释约1046ms，`qwen_explain=success`。证据保存于 `artifacts/e2e/real_agent_validation_sample_success.json`。
- 最终 E2E 样本的预测为“正常”70.51%，当前纯置信度规则给出 `requires_review=false`；但“正常”类 OOF Recall=0/7。产品安全协议不能忽视类别级验证证据，应把“正常”和样本极少的“乱建”设为强制复核类，避免大模型把统计上不可靠的结论表述为“无需复核”。
- 类别风险复核策略已落地并完成真实 E2E：同一“正常”70.51%结果现在为 `requires_review=true`，理由是“正常类验证样本仅7张且OOF召回率为0%”；Qwen解释约1.24秒并明确建议人工复核。该规则只影响交互提示，不改变赛事批量预测标签。
- Gradio演示层已按架构实现，可展示回答、分类/置信度、复核原因、Top-3和工具轨迹；SQLite审计仅存请求ID、时间、结构化结果、模型版本与耗时，不保存图片、路径、问题正文或密钥。
- 模型版本标识由含糊的 `best+best+best+best` 改为四个训练运行目录名与权重文件名，便于演示和审计追溯。

## Phase 4 现场验收发现（2026-08-20）

- FastAPI在关闭审计、使用Fake视觉模型时现场启动成功。`GET /health`返回200并正确报告`vision_mode=fake`、`qwen_enabled=false`。
- 对训练来源验证副本的`POST /api/v1/classify`与`POST /api/v1/analyze`均返回200，Schema包含六类标签、Top-3和工具轨迹；Fake模型结果只用于接口契约，不用于模型成绩。
- 上传扩展名为`.txt`时返回415“仅支持JPG、PNG或WebP图片”；伪造`.jpg`但内容不可解码时返回415“文件不是可解码图片”。说明扩展名和实际解码两层校验均生效。
- 真实FastAPI以四折集成、官方Qwen和SQLite审计启动成功；health正确报告`vision_mode=ensemble`、`qwen_enabled=true`、`audit_enabled=true`。
- 同一验证副本连续5次分类均稳定输出“正常”。首个请求HTTP约548ms/模型约536ms，后4次HTTP为84.5–95.8ms；5次HTTP中位数90.39ms，模型中位数82.77ms，说明模型加载后热推理已进入百毫秒内。
- 连续3次真实Agent分析均200、复核策略均生效、视觉/Qwen/审计轨迹均success。端到端HTTP分别约1795、1491、1274ms，中位数1491.46ms；Qwen解释约1178–1702ms，SQLite单次审计约7.7–9.8ms。
- 真实审计库共3条记录，字段仅含请求ID、时间、标签、置信度、复核信息、模型版本、总耗时和轨迹JSON；不存在prompt、question、image/path、answer、api_key或authorization字段。四折训练运行名均完整记录，可追溯性通过。
- 3个并发分类请求均返回200且结果一致；单请求墙钟时间约395–530ms，相比顺序热请求约85–96ms明显增加，说明当前单进程GPU推理存在资源争用。首版适合低并发演示与监管单图分析，不应宣称高吞吐服务。
- 修改后的Gradio在7862端口启动成功；官方Client发现2输入/5输出的`/lambda`端点。使用训练来源验证副本完成回调约611.52ms，回答、分类/置信度、复核原因、Top-3 Markdown表和工具轨迹表全部非空，证明页面后台功能链闭环。
- Fake UI本次输出“乱建”29.75%并触发低置信度复核，仅用于验证页面协议；不能作为模型识别结果。Markdown替换已消除Polars后处理异常。
- 修复后的7862页面由浏览器真实渲染：标题、说明、图片上传、默认问题、开始分析、回答、分类、复核、Top-3和工具轨迹均可见；Top-3/轨迹已变为语义清晰的三级标题与Markdown内容，不再存在Dataframe/Polars依赖。
- 修复后页面控制台无error/warning。真实API进程现场工作集约1.32GiB、私有内存约3.53GiB；`nvidia-smi`报告系统总GPU显存占用约1377MiB/8188MiB、采样时GPU利用率29%。Windows驱动未提供单进程显存数，因此不把1377MiB全部归因于本服务。

## Phase 5 交付约束（2026-08-20）

- 官方根目录模板`AI智能体设计方案.docx`是方案文档的结构与视觉权威；最终文档须从模板副本编辑，原文件保持不变。
- 提交结构由官方`readme.txt`固定为`code/`、`design/`、`result/result.json`，最终归档格式为`.tar.gz`。
- 当前根目录存在训练数据、测试数据、API密钥、训练产物和缓存，不能采用整目录打包；必须使用显式白名单复制，并在归档前扫描敏感文件名和结果Schema。
- 由于测试集由用户独占操作，开发阶段只能完成构建器和临时测试，不能生成或声称存在真实最终提交包。
- 模板SHA-256为`8e8148db47a1fd7050c11027105f85299c72596ffaefd4c9a700824afc9877d0`；结构是单节A4纵向，标题+三项元数据+五章正文槽位，共16个顶层段落，无表格、图片、域或内容控件。
- 标准渲染器在专用可写目录仍因系统找不到`soffice`失败，已满足技能定义的“LibreOffice缺失”回退条件；后续结构核验必须保留该限制，不能宣称逐页PNG视觉验收通过。
- 当前代码已具备提交目录验证器，但还没有最终归档构建器；新增构建器应复用`validate_submission`/`validate_delivery`，并通过白名单而不是排除列表组装代码。
- 方案DOCX已从官方模板副本生成：82个顶层段落、4个表格、1个A4节、14个包部件，原模板SHA-256保持不变；可访问性审计高/中/低问题均为0。
- 模板没有Heading/List/Table样式或编号部件，最终文档有意延续模板的Normal+直接格式体系；列表源被转换为分组正文，表格使用显式DXA几何和直接OOXML边框。
- 方案文档仍有1处明确队伍名槽位，必须由用户提供报名队伍名后再生成最终身份版本；当前环境的LibreOffice缺失仍使逐页视觉QA待外部Word/LibreOffice完成。
- 用户已确认报名队伍名为`X-M`；该值应替换方案DOCX中的唯一身份占位符，并由提交构建器重新验证占位符已消失。

- 1 epoch类别加权对照（power=0.5、cap=8）将Balanced Accuracy从0.4654提升到0.5778，乱采Recall从0.3333升至0.6667、乱堆从0.6667升至1.0；但Accuracy降至0.9161、Macro-F1降至0.4919，且乱建/正常仍为0。该结果支持继续做收敛期对照，但不支持直接将此权重策略定为最终方案。
- 下一次A/B固定同一预训练权重、fold、随机种子、分辨率、batch和epoch，仅改变类别损失；用Macro-F1与Balanced Accuracy联合选择，Accuracy作为误报代价约束。
- 15 epoch公平A/B：标准损失Accuracy 0.9895、Balanced Accuracy 0.8222、Macro-F1 0.8266；加权损失分别为0.9930、0.8333、0.8327。加权模型多识别正确1张“乱占”，其余五个异常类在本折均全对，因此暂选加权策略进入四折。
- 两个15 epoch模型对2张“正常”均为0 Recall，全部误判为“有漂浮物”；加权使其中一张的多数类置信度从0.9401降到0.8293，但并未提升“正常”概率。需要用四折OOF检查这是普遍模式还是fold 0偶然现象。
- 单折只有1张乱建、2张正常；当前83%左右Macro-F1受“正常”F1=0强烈影响。任何最终性能主张都必须以四折OOF为准。
- 四折加权模型OOF完整覆盖1,144张且无重复：Accuracy 0.9773、Balanced Accuracy 0.7111、Macro-F1 0.7343、Top-2 Accuracy 0.9895。
- OOF每类Recall：乱采1.0、乱建0.5、乱堆0.8261、乱占0.95、有漂浮物0.9903、正常0。正常7张中6张误为有漂浮物、1张误为乱堆；同时有3张有漂浮物被误报为正常，简单提高“正常”先验无法解决问题。
- 温度T=1.1603将NLL从0.1266降至0.1240，但Brier从0.0418升至0.0437、ECE从0.0157升至0.0190。首版不启用该温度校准，保留产物供后续研究。
- 最终测试推理采用四折模型等权概率平均；OOF无法无泄漏评估该集成增益，因为每个训练样本只对其所属验证折模型是未见样本，其他三模型均见过。
| Decision | Rationale |
|----------|-----------|
| 暂不冻结 YOLO 版本和智能体框架 | 尚未获得评分公式、决赛资源和详细部署约束 |
| Agent 大模型基线采用赛事 Qwen3.6-27B 接口 | Q&A 明确官方提供，且符合国产开源模型限制；仍通过适配层避免绑定 |
| 视觉模型作为独立工具接入 Agent | 与用户原始思路一致，并得到 Q&A 第13条明确认可 |
| Agent采用自研WaterAnalysisAgent受控编排 | 官方接口未证明Function Calling；确定性调用视觉工具更可复现，并保留Qwen解释与故障降级 |
| Web后端选用FastAPI，演示界面选用Gradio Blocks | 后端接口清晰，Gradio可快速完成图像+对话+过程展示，避免独立前端工程 |
| 主视觉模型采用Ultralytics YOLO分类小模型 | 官方标签是整图单分类，本机8GB显存适配小模型；通过模型适配器保留替换空间 |
| 训练和批量推理采用CLI，Web只做单图/轻量任务 | 避免GPU训练阻塞Web进程，也减少Redis/Celery等额外运维组件 |
| Qwen VL Embedding/Reranker作为可关闭的增强模块 | 可用于低置信度相似案例检索，但不阻塞核心YOLO+Agent链路交付 |
| API客户端按OpenAI兼容协议实现 | 官方示例通过ChatOpenAI调用统一 `/v1` 基地址，可复用成熟消息与流式协议 |

- 文档一致性审计发现早期架构仍写“Qwen-Agent框架”和`yolo26s-cls`，与实际实现不符；现已统一为自研`WaterAnalysisAgent`受控编排和四折`yolo26n-cls`集成，避免评委按不存在的依赖复现。
- Phase 4最终质量门为Ruff全绿、32项pytest通过、`pip check`无冲突；CLI已注册`validate-delivery`。完整工程证据汇总于`acceptance_report.md`。

## Issues Encountered
| Issue | Resolution |
|-------|------------|
| 参赛 Q&A 的微信文档链接被浏览器站点安全策略拦截 | 未使用其他浏览器或间接方式绕过；用户随后直接提供正文，已完成规则核对 |
| `AI智能体设计方案.docx` 无法渲染为页面图片，环境未找到 LibreOffice/soffice | 依照文档读取回退流程，改做 OOXML/结构化文本与表格提取；涉及页码或纯视觉信息的判断保持未核验 |
| 首次 DOCX 结构化提取的中文在终端中乱码 | 修改只读提取脚本，使其显式以 UTF-8 输出后再读取；不改动原始 DOCX |
| 当前工作目录不是 Git 仓库 | 需求阶段不受影响；若进入实现阶段，应先由用户确认是否在此目录初始化版本控制 |
| CPU/内存WMI查询被系统拒绝 | 不请求提升权限；以已获取的GPU信息进行首轮选型，部署前再用可用方式核验 |
| 当前终端没有系统Python命令 | 创建项目级Python环境并提供可复现安装说明 |
| 首次代表样本路径抽取PowerShell出现空管道语法错误 | 改为先把循环结果收集到数组，再单独格式化输出，不重复失败写法 |
| 第二次代表样本查询在数组计数减法处解析失败 | 缩小目标为每类首个样本，不再进行复杂索引计算 |

## Resources
- 报名链接：https://iaic.cqaip.cn
- 参赛 Q&A：https://doc.weixin.qq.com/doc/w3_ASEA7Qa_AA0CNxf91YT1mSDOTWlU2?scode=ABUAtgeIAGcQN225Z9
- 本地资料：`E:\GPT-Codex\Yolo-water\水域综合异常识别`
- Qwen3.6-27B 官方模型仓库：https://huggingface.co/Qwen/Qwen3.6-27B
- Qwen-Agent官方仓库：https://github.com/QwenLM/Qwen-Agent
- Ultralytics分类任务官方文档：https://docs.ultralytics.com/tasks/classify
- FastAPI后台任务说明：https://fastapi.tiangolo.com/tutorial/background-tasks/
- Gradio Chatbot文档：https://gradio.app/main/docs/gradio/chatbot

## Visual/Browser Findings
- 报名系统首页当前可正常访问，水域赛道标识为 TRACK_03，状态显示“报名开放中”。
- 官网的赛道链接 `/tracks/smart-terminal` 直接导航后会回到 `/home`，没有暴露独立赛道详情；当前能核验到的水域赛道网页信息仅为首页卡片与通用赛程。
- 未登录状态下，`/leaderboard` 与 `/registration` 也都会返回首页，因而无法从公开页面核验评分字段或更详细的报名规则。

## Phase 10：准确率与人工纠错闭环调研（2026-08-21）

- 当前官方训练标签极不均衡：乱建4、正常7、乱堆23、乱采24、乱占60，而有漂浮物1026。单纯以相同数据重复训练，可能因随机性产生波动，但不能可靠地解决乱建、正常等信息不足问题。
- Ultralytics 分类默认训练使用随机裁剪、验证/推理使用中心裁剪；水域异常常位于岸线、角落或小区域，必须以同一组隔离OOF切分验证“保留完整画幅（letterbox）/多裁剪”和更高输入分辨率，不能直接假定其一定更好。来源：Ultralytics分类文档与增强参考。
- 长尾候选包括有效样本数的Class-Balanced Loss、Balanced Softmax、LDAM-DRW，以及“先普通训练表征、再均衡重训分类头”的decoupling策略。它们均须作为单变量对照；不应在4或7张样本类别上承诺稳定提升。
- 现有四折模型可输出模型间分歧、置信度和Top-1/Top-2间隔；这三项可组成主动学习队列，再加入向量嵌入的多样性抽样，使人工时间优先投向高价值纠错样本，而不是随机收集。
- 人工反馈只能形成“候选标注数据”，不能让在线模型立即改参数或让LLM自行改标签。应经过标签确认、去重、来源/授权、冻结验证集和离线冠军-挑战者比较后才允许再训练。
- FloW等公开数据集主要覆盖水面漂浮垃圾，且访问/许可与标签定义需逐项确认；它不能直接解决乱建、乱采、乱占的场景语义问题，也不能未经映射直接并入官方六分类训练集。
- 用户已进一步收紧当前优化边界：不使用外部数据集；所有新训练/验证试验仅使用赛事方提供的官方训练集。后续研究与实验将围绕预训练权重、官方训练图的增强/重采样、长尾损失、图像变换、四折OOF和模型集成展开。

## Phase 10 P0：实验基线与输入链路复核（2026-08-21）

- 当前冠军四折OOF基线：Accuracy 0.97727、Balanced Accuracy 0.71106、Macro-F1 0.73432；弱类Recall为乱采1.0、乱建0.5、乱堆0.82609、乱占0.95、正常0。该基线是后续候选是否保留的最低比较口径。
- 现有fold 0公平基线使用`yolo26n-cls.pt`、320、batch 16、15 epoch、seed 20260820、类别权重power=0.5/cap=8；验证时使用Ultralytics分类默认中心裁剪，训练时使用随机裁剪、RandAugment、随机擦除0.4和水平翻转0.5。
- 当前安装的Ultralytics为8.4.123；其`ClassificationDataset`实际模块在`ultralytics.data.dataset`，首次按旧路径导入失败，后续自定义全图变换必须以该版本实际接口实现。
- RTX 4070 Laptop GPU总显存8188 MiB，采样时已用1336 MiB；512实验从batch 8开始，避免影响正在运行的用户服务或出现显存不足。

## Phase 10 P1：512默认裁剪单变量实验（2026-08-21）

- 实验仅使用官方训练集的隔离fold 0副本（858训练、286验证），`yolo26n-cls`、15 epoch、seed 20260820、类别权重power=0.5/cap=8保持不变；唯一算法变量为输入尺寸320→512，batch按显存从16降为8。
- 512候选在fold 0评估为Accuracy 0.98951、Balanced Accuracy 0.83268、Macro-F1 0.74935；原320加权基线为0.99301、0.83333、0.83268。因此候选未达到保留门槛，不进入四折复训或打榜。
- 512候选逐类Recall：乱采1.0、乱建1.0、乱堆1.0、乱占1.0、有漂浮物0.99609、正常0。它以更多“有漂浮物→乱建”误报换得了乱建单样本正确，且仍未解决正常类，不能视为稳定改进。

## Phase 10 P2：全图Letterbox单变量实验（2026-08-21）

- 实验仅使用官方训练集的隔离fold 0副本（858训练、286验证），`yolo26n-cls`、320、batch 16、15 epoch、seed 20260820、类别权重power=0.5/cap=8保持不变；唯一图像策略变量为“训练与验证均采用全图等比例缩放后补边（letterbox），训练阶段保守水平翻转”。
- P2在fold 0独立评估为Accuracy 0.98951、Balanced Accuracy 0.82222、Macro-F1 0.81412，均低于320默认裁剪冠军折基线的0.99301、0.83333、0.83268。因此P2淘汰，不进入四折复训、模型集成或正式提交。
- P2逐类Recall：乱采1.0、乱建1.0、乱堆1.0、乱占0.93333、有漂浮物1.0、正常0。混淆为1张乱占→乱堆、2张正常→有漂浮物。它没有解决正常类，且损害乱占，故“保留完整画幅”在本官方数据切分上不是有效优化方向。
- P2训练过程稳定完成15个epoch；最佳内部top-1为0.98951。最终评估耗时约34.3秒、286张验证图平均119.9毫秒/张。所有产物存放在Git忽略的`experiments/`，没有读取初赛测试集。

## Phase 10 P3a：Logit Adjustment单变量实验（2026-08-21）

- 实验使用默认320裁剪、官方训练集隔离fold 0副本、15 epoch、seed 20260820和既有类别计数；唯一算法变量为训练期`tau=0.25`的类别先验Logit Adjustment。推理阶段未添加logit偏置，避免人为改变线上输出尺度。
- P3a独立评估为Accuracy 0.98951、Balanced Accuracy 0.83268、Macro-F1 0.82698；相对冠军fold 0基线0.99301、0.83333、0.83268，平衡准确率近似持平，但总准确率和Macro-F1均下降，因此淘汰。
- P3a逐类Recall：乱采1.0、乱建1.0、乱堆1.0、乱占1.0、有漂浮物0.99609、正常0。混淆为1张有漂浮物→乱占、2张正常→有漂浮物。该校正没有改善正常类，不能进入四折复训或提交。
- 训练过程确认Windows Polars CPU检查会在保存权重时误报`sse3`；`POLARS_SKIP_CPU_CHECK=1`已本地验证可安全绕过该环境误判。训练入口已仅在Windows设置该进程环境变量。
