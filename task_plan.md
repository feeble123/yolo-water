# 水域综合异常识别智能体项目计划

## Goal
在仅只读使用训练集、且完全不访问初赛测试集的边界下，交付可打榜、可演示的“水域综合异常识别”行业智能体：以图像分类模型完成异常识别，以国产开源大模型编排工具并生成面向普通用户的解释。

## Current Phase
智能体化升级（Phase 6–9）已完成；Phase 10正在进行受控的单变量模型优化实验

## Phases

### Phase 1：需求与赛事调研
- [x] 核验报名页面中公开可见的赛事规则、赛程与提交要求
- [x] 核验参赛 Q&A 中与“水域综合异常识别”相关的动态说明
- [x] 盘点本地赛事资料（明确排除“训练集”和“初赛测试集”目录）
- [x] 评估“YOLO 视觉工具 + 智能体编排”的可行性与合规性
- [x] 整理功能、非功能、数据、验收、交付和待确认需求
- [x] 与用户评审并冻结需求基线
- **Status:** complete

### Phase 2：系统与技术方案设计
- [x] 定义总体架构、接口和技术选型
- [x] 制定数据、模型、智能体、前后端及部署设计
- [x] 完成风险与测试策略设计
- [x] 形成10天压缩交付计划与项目骨架
- **Status:** complete

### Phase 3：实现
- [x] 实现 YOLO 数据审计、分组四折、隔离验证与GPU训练能力
- [x] 实现评估器并产出Macro-F1、Balanced Accuracy、每类指标和混淆矩阵
- [x] 建立多数类基线并完成单折长尾策略对照
- [x] 完成四折训练、概率集成与置信度校准
- [x] 实现赛事批量预测与用户本地运行说明
- [x] 实现智能体、工具调用与API纵向切片（Fake视觉模型）
- [x] 复核官方图像级标签与四折 OOF Recall 证据链，修正“检测/分类”术语
- [x] 安全验证官方 Qwen3.6-27B 接口并完善错误处理
- [x] 使用四折 YOLO-CLS 集成完成真实 Agent 端到端验证
- [x] 实现必要的日志、配置和结果展示能力
- **Status:** complete

### Phase 4：测试与赛事验证
- [x] 执行单元、集成、官方Qwen连通和真实Agent端到端测试
- [x] 现场启动FastAPI并验收health、classify、analyze及无效上传
- [x] 现场启动Gradio并验收页面结构、上传交互与结果展示
- [x] 测量冷启动、单图视觉、Qwen解释与端到端耗时并形成报告
- [x] 验证Qwen故障降级、SQLite审计脱敏和并发/重复请求稳定性
- [x] 对照赛事规则验证提交物与演示流程
- [x] 修复问题并形成可复现证据
- **Status:** complete

### Phase 5：交付与参赛材料
- [x] 完成可复现部署配置与运行脚本
- [ ] 按官方模板完成智能体方案设计文档（内容、结构和队伍名已完成；仅待外部逐页视觉复核）
- [x] 准备演示脚本、截图/录屏清单和模型卡
- [x] 组装`code/ design/ result/`提交目录并生成`.tar.gz`
- [x] 完成最终合规检查与交付清单
- **Status:** in_progress

### Phase 6：智能体化升级——需求冻结与安全设计
- [x] 核验当前实现：Qwen仅负责YOLO结果的事后解释，未参与工具选择；将其定义为“轻量工具封装”基线
- [x] 冻结升级目标、用户可见交互、非目标与成功标准
- [x] 定义受控工具集、结构化任务计划协议、知识库边界与数据安全约束
- [x] 定义与现有赛事批量链隔离的兼容策略和回退策略
- **Status:** complete

### Phase 7：智能体化升级——Qwen编排器与工具层
- [x] 实现Qwen驱动的结构化意图/任务计划生成器；无Qwen、超时或非法输出时安全回退到确定性计划
- [x] 将YOLO识别、类别知识查询、巡查报告生成和审计记录建模为独立工具契约
- [x] 实现受控执行器：只执行白名单工具，视觉标签只能由YOLO工具写入
- [x] 增加“识别 / 巡查报告 / 类别说明 / 复核建议”等用户意图，并让不同意图产生不同工具链
- **Status:** complete

### Phase 8：智能体化升级——交互、接口与可见证据
- [x] 将网页从单次表单升级为任务式对话界面，展示“智能体理解的任务、执行计划、已调用工具、证据和结论”
- [x] 保留结构化分类、Top-3、置信度和人工复核提示；不把模型推测伪装成事实
- [x] 扩展REST接口并保持既有`/api/v1/analyze`兼容
- [x] 明确显示Qwen是否已启用、是否实际调用成功、以及故障回退原因
- **Status:** complete

### Phase 9：智能体化升级——验证、演示与交付
- [x] 覆盖正常路径、Qwen关闭/失败、非法计划、图片缺失、低置信度和强制复核等单元与集成测试
- [x] 使用真实官方Qwen接口做安全分段验收；不输出或写入API密钥
- [x] 更新架构、部署说明、模型卡、设计方案和4–5分钟演示稿
- [x] 现场验证网页端与API端，并形成可复现演示证据
- **Status:** complete

### Phase 10：榜单准确率优化（与智能体升级分离）
- [x] 在不访问初赛测试集的前提下，根据81.59榜单反馈建立弱类优化实验矩阵与人工纠错闭环设计
- [x] 冻结本轮约束：不引入外部数据；只使用官方训练集、既有开源预训练权重和固定四折近重复组隔离口径
- [x] P0：复核当前训练/推理变换、基线权重和GPU资源；建立实验命名与产物隔离规则
- [x] P1：在fold 0执行320→512的高分辨率单变量对照，保持YOLO、类别加权、随机种子和epoch一致（结果不达标，淘汰）
- [x] P2：验证全图letterbox训练/验证与保守水平翻转增强（fold 0结果不达标，淘汰）
- [ ] P3：以最优图像策略为基础，顺序比较Logit Adjustment、LDAM-DRW和两阶段分类头再均衡
- [ ] P4：完成四折OOF复训、TTA/模型概率集成及逐类混淆矩阵验收；只保留胜过当前冠军的版本
- [ ] 每轮只改变有限变量，记录训练集分组OOF指标、类别指标、提交版本和榜单反馈
- [ ] 仅在用户授权后生成新的`result.json`和正式提交包
- **Status:** in_progress（P0–P2已完成；P1、P2均未胜过冠军基线，未改动提交结果）

## 智能体化升级的冻结方案

### 目标
将当前“YOLO分类网站 + Qwen结果改写”升级为**受控多工具行业智能体**。它应能理解用户任务，生成并展示执行计划，按任务选择YOLO视觉识别、类别知识查询、巡查报告生成、人工复核建议与审计等工具；视觉事实仍由YOLO给出，LLM不得改写类别。

### 用户可见能力
| 用户需求示例 | 预期工具链 | 输出重点 |
|---|---|---|
| “这张图有什么异常？” | 视觉识别 → 结论生成 | 类别、置信度、Top-3、依据边界 |
| “生成巡查处置建议” | 视觉识别 → 类别知识查询 → 报告生成 | 疑似事件、复核项、建议动作、风险提示 |
| “乱堆和乱建有什么区别？” | 类别知识查询 | 分类定义、易混淆点、不可替代现场核实的边界 |
| “这张结果可靠么？” | 视觉识别 → 复核策略 → 解释 | 置信度、弱类风险、是否需人工复核 |

### 非目标与硬约束
- 不让Qwen直接替代YOLO判图，不允许其修改YOLO标签、伪造框、数量、地点或违法事实。
- 不访问、枚举或推理初赛测试集；不将图片、问题正文、密钥写入审计库或提交包。
- 不改变已验证的赛事批量预测输入、四折模型、`result.json`生成逻辑；智能体升级与打榜链物理/逻辑隔离。
- 不声称目标检测、违法认定、自动执法、视频实时告警等当前没有数据或功能支撑的能力。

### 架构决策
1. 采用“Qwen结构化计划 + 本地白名单执行器”，而非依赖未验证的远程Function Calling。
2. Qwen只输出受Pydantic校验的任务意图和建议工具序列；解析失败、超时或未启用时使用确定性计划。
3. 工具结果采用只读结构化证据对象。YOLO标签字段由视觉工具独占写入；Qwen仅可读取并生成说明文本。
4. 知识库首版只使用项目内版本化的赛题标签说明、模型边界和非执法性巡查建议；后续接入官方Embedding/Reranker前先做无密钥泄露的接口契约验证。
5. 网页显式展示任务计划和工具轨迹，使“智能体正在做什么”对演示者可见，而不是只展示最终一句话。

### 验收标准
- 同一张图在“识别”和“巡查报告”任务下，YOLO分类结果保持一致，但工具计划和报告内容明显不同。
- Qwen启用时，轨迹中出现`plan_task`与`generate_report`成功记录；Qwen不可用时，页面明确显示确定性回退，YOLO仍可用。
- 任何Qwen输出都不能修改结构化视觉类别；通过自动化回归测试验证。
- UI、API、测试、文档和演示稿均能说明并证明上述链路；原赛事批量预测回归通过。

## Key Questions
1. 官方对智能体形态、模型来源、联网能力、部署环境、提交格式和评分项的明确约束是什么？
2. “水域综合异常识别”要求识别哪些类别，输出检测框、分类、计数、严重度还是处置建议？
3. 官方数据是否已经标注，若只有图片，标注由谁完成、采用何种规范？
4. 比赛验收更重视模型精度、智能体自主性、工程完整度、创新性还是演示效果？
5. 最终系统是单图问答原型，还是还需支持批量图片、视频流和监控接入？
6. 目标运行环境的 GPU、操作系统、网络和大模型 API 条件是什么？

## Decisions Made
| Decision | Rationale |
|----------|-----------|
| 当前仅开展需求调研，不进入设计和实现 | 用户明确要求按严格软件流程推进，现阶段先把需求聊透 |
| 训练集仅只读使用；不打开、枚举或以任何方式访问“初赛测试集” | 用户最新明确的数据访问边界 |
| 外部页面内容只写入 findings.md | 遵守调研资料与执行计划隔离原则，降低外部内容干扰 |
| 需求定义为“可替换视觉识别工具”，暂不锁死 YOLO 目标检测 | 官方是整图单标签数据，无检测框；需先保证赛事分类目标，再验证定位增强 |
| 首版建议聚焦单图问答与赛事批量推理 | 直接覆盖用户核心场景与初赛 JSON 提交；视频流和告警平台作为后续扩展 |
| 对外统一表述为“水域异常识别” | 官方六类不仅包含污染/漂浮物，还包含乱采、乱建、乱堆、乱占等治理事件 |
| 采用“国产开源大模型 Agent + 自研视觉模型工具”作为合规架构基线 | Q&A 明确初赛只能使用国产开源大模型，并明确认可将自行开发的深度学习模型包装为 Agent 工具 |
| Agent 默认对接赛事提供的 Qwen3.6-27B 接口，但保留模型适配层 | 官方提供该模型接口，同时允许选手自选其他开源模型；适配层避免绑定单一服务 |
| 外部训练数据仅使用可公开追溯的数据并建立来源清单 | Q&A 允许扩展外部数据，但要求公开、标注来源，并禁止违规、涉密、私有及商用受限数据 |
| 将完整落地演示纳入首版交付目标 | 评审重点不仅是模型分数，还包括实用性、场景适配和完整解决方案；后续需提交运行演示材料 |
| 采用压缩式设计并立即转入实现 | 距离8月底仅约10天，用户明确授权不再等待多轮原型；通过纵向切片和功能开关控制风险 |
| 一套视觉模型提供交互链与批量评测链 | Agent链体现智能体能力，确定性CLI链保证刷榜稳定；两条链共用模型与Schema，减少重复开发 |
| 训练集允许只读访问，测试集由用户独占操作 | 用户明确授权训练与验证，但禁止Agent查看测试集；提供用户本地运行的预测/校验CLI满足边界 |
| API密钥只从既有密钥文件或环境变量加载 | 防止密钥进入代码、日志、计划文件、模型产物或提交包 |
| 单图分析采用受控本地Agent编排 | 官方示例未证明Function Calling；分析端点的视觉工具意图明确，确定性调用更可复现、可审计，Qwen只解释结构化结果 |
| 下一阶段先评估、后做长尾对照、最后四折长训练 | 冒烟Top-1受89.69%多数类支配，必须用Macro-F1与每类Recall驱动优化，避免浪费有限训练时间 |
| 最终视觉工具采用四折加权模型等权概率平均 | 单折A/B中加权损失小幅全面胜出；概率平均不依赖类别索引偶然顺序，并可同时服务Agent与批量提交链 |
| 首版温度固定为1.0，不启用OOF拟合的T=1.1603 | 拟合温度仅改善NLL，却恶化Brier和ECE；不应以单一校准指标决定上线 |
| 下一实现顺序为官方Qwen API安全实测、真实Agent端到端、界面与演示材料 | 视觉模型与确定性批量链已闭环，当前剩余主要风险转为官方接口兼容性和作品展示完整度 |
| 最终提交包采用“严格白名单构建 + 真实result.json强校验” | 防止训练/测试数据、API密钥、缓存和内部日志进入作品；在用户生成真实测试结果前拒绝产出最终`.tar.gz` |
| 方案身份信息不做推断，构建器阻断占位符 | 报名队伍名属于用户事实；未知时明确留槽，比虚构身份或静默遗漏更安全 |

## Errors Encountered
| Error | Attempt | Resolution |
|-------|---------|------------|
| 微信文档 Q&A 被浏览器站点安全策略阻止访问 | 1 | 未绕过；用户随后直接提供 Q&A 正文，规则缺口已补齐 |
| DOCX 渲染器未找到 LibreOffice/soffice | 1 | 不重复相同命令；改用结构化 DOCX 内容提取，并明确视觉页码核验缺失 |
| DOCX 结构化提取输出中文乱码 | 1 | 调整提取脚本为显式 UTF-8 输出后重试 |
| `git status` 报告当前工作区不是 Git 仓库 | 1 | 当前仅需求调研，不依赖版本控制状态；停止 Git 检查并记录该环境事实 |
| Windows系统拒绝WMI/CIM读取CPU与内存 | 1 | GPU信息已足够进行模型选型；不绕过权限，CPU/内存作为部署前待核验项 |
| 当前终端未发现可直接调用的Python命令 | 1 | 实现阶段创建项目独立Python环境，并显式记录版本与依赖 |
| 代表样本路径抽取PowerShell出现空管道语法错误 | 1 | 改用数组收集循环结果后再输出 |
| 修改后的代表样本脚本发生PowerShell属性表达式解析错误 | 2 | 放弃中位/末尾索引计算，改为每类直接选择首个样本的简单查询 |
| Fake FastAPI首次现场启动因`artifacts/acceptance`目录ACL拒绝创建SQLite | 1 | 不重复原命令；接口契约验收关闭审计，审计持久化改用独立受控测试或已获权限的目录 |
| PTY中的Uvicorn未响应两次Ctrl+C | 1 | 核对现场启动日志中的精确PID后停止该单一Python进程，并确认进程已退出 |
| API集成测试收集时默认审计库不可写导致模块导入失败 | 1 | 审计初始化失败时安全降级，`/health`新增实际审计状态，核心API不再被可选存储阻断 |
| 浏览器运行时不支持`waitForLoadState(networkidle)` | 1 | 改用受支持的`domcontentloaded`并通过DOM可见元素确认Gradio完成渲染 |
| 点击Gradio隐藏文件input未触发浏览器文件选择器 | 1 | 浏览器会话重连后改点可见的`Upload file`/上传按钮，并用try/catch保护事件等待 |
| 点击Gradio可见`Upload file`仍未产生文件选择器事件 | 2 | 停止重复浏览器上传；页面视觉结构由浏览器验收，功能回调改用Gradio官方Client API端到端验证 |
| Gradio Client回调因Dataframe后处理导入Polars失败 | 1 | Top-3和工具轨迹改为Markdown表格，消除Polars/CPU特征检测依赖并保留展示信息 |
| 修改UI后7861端口仍被残留监听占用 | 1 | 不重复占用检查结果；重启验收服务改用7862端口，旧会话在阶段收尾统一清理 |
| 验收报告批量补丁因findings历史行上下文不匹配而未应用 | 1 | 拆分架构、决策、发现和新报告为独立小补丁，避免跨文件原子失败 |
| DOCX模板QA目录无法在遗留`artifacts/`下创建 | 1 | 该目录沿用训练阶段ACL；不重复写入，模板渲染与中间证据改放到当前会话专用可写QA目录 |
| 内联Python模板检查命令被PowerShell转义为字面量换行符 | 1 | 改用版本化只读检查脚本，避免继续叠加不可靠的shell转义 |
| 官方DOCX模板缺少`List Bullet`/`List Number`样式和编号部件 | 1 | 不伪造Unicode项目符号；方案构建器把Markdown列表转换为有缩进的分组正文，保留顺序与标签层次 |
| Markdown表格分隔行正则中的连字符范围写法无效 | 1 | 改为逐单元格校验`:?---+:?`，避免不明确字符类并保持严格表格识别 |
| 官方模板缺少`Table Grid`表格样式 | 1 | 不引入第二套样式包；直接写入浅色单线边框、显式DXA列宽/缩进/单元格宽度和内边距 |
| `python-docx`不接受把单元格宽度赋值为`None` | 1 | 删除无效对象层赋值，只通过OOXML `tcW`与`tblGrid`设置精确DXA宽度 |
| 新增文档构建脚本首次Ruff检查有2项规则问题 | 1 | 合并`startswith`条件并改用显式UTC日期；同时保留模板直格式为预期行为 |
| 当前项目虚拟环境尚未同步新增`python-docx`依赖 | 1 | 代码声明已加入`pyproject.toml`；下一步重新安装editable项目并复跑`pip check` |
| `pip show ultralytics`在GBK终端打印火箭emoji触发Rich日志编码错误 | 1 | 不再使用富文本`pip show`采集许可证；改用`importlib.metadata`读取机器可解析字段 |
| 沙箱内editable依赖同步因网络限制无法下载hatchling | 1 | 按环境规则获得联网权限后成功安装新增依赖并刷新editable项目 |
| DOCX表格几何审计发现缩进120 DXA与起始单元格边距100 DXA不一致 | 1 | 将表格缩进改为100 DXA后重新生成文档并复跑几何审计 |
| Windows PowerShell 5.1将UTF-8无BOM脚本中的中文提示误解码并触发ParserError | 1 | 将全部运行`.ps1`提示改为纯ASCII，并新增Windows PowerShell 5.1解析门 |
| 首次正式打包将空白`.env.example`误判为已赋值密钥 | 1 | 将配置赋值扫描的空白限定为水平空格，禁止跨行吞入下一配置项，并新增空值回归测试 |
| 第二次打包将Python运行时`api_key=secret.get_secret_value()`误判为硬编码密钥 | 2 | Python源码仅拒绝凭据名后的字符串字面量，配置文件继续拒绝任意非空赋值；新增动态读取允许门和字面量拒绝门 |
| 独立归档审计脚本括号笔误导致首次只读检查未执行 | 1 | 修正一次性审计表达式后重跑，归档未修改，最终全部审计通过 |
| Gradio界面在无`.env`时静默使用Fake视觉模型，导致真实图片结果与标签不符 | 1 | `start_ui.ps1`默认配置四折真实YOLO、320尺寸和项目内运行缓存；Fake仅允许显式开关，页面展示实际模式，Qwen显式注入密钥文件 |
| 真实界面启动时Ultralytics写系统目录、Matplotlib读取缺失`WINDIR`失败 | 2 | 启动脚本固定`YOLO_CONFIG_DIR`、`MPLCONFIGDIR`到项目`runtime/`，并在缺失时恢复`C:\Windows` |
| 临时7867服务清理首次使用PowerShell只读`$PID`变量失败 | 1 | 改用非保留变量名精确停止7867监听PID；确认7865用户服务未受影响 |
| Phase 10 P1训练向历史`artifacts/training`写入新目录被Windows ACL拒绝 | 1 | 不覆盖旧训练产物；创建项目内独立可写`experiments/`目录，用于本轮训练、指标与运行缓存 |
| Phase 10 P1以前台命令运行时达到环境执行时限，只完成第1个epoch | 1 | 半成品不评估、不覆盖；改为`Start-Process -WindowStyle Hidden`后台训练并通过独立日志和results.csv轮询 |
| Phase 10使用Ruff默认`.ruff_cache`被历史目录ACL拒绝；首次改缓存后相对源码路径解析失败 | 2 | 单元测试已通过；后续Ruff改用可写`experiments/runtime/ruff`与绝对文件路径执行，避免依赖当前进程目录 |

## Notes
- Phase 1 结束条件是：赛事要求与本地资料已核对、可行性边界明确、需求清单已由用户确认。
- 用户已授权转入压缩式设计与实现，并允许只读访问训练集。
- 初赛测试集禁止Agent打开、列目录、读取元数据或执行推理；最终预测与提交校验由用户在本地终端运行项目CLI。
- 参赛 Q&A 正文已由用户在 2026-08-20 直接提供，原微信文档访问缺口已解决。
